# 调参命令

无线转串口模块，UART2 @115200。模块 RX 接 `P10_5`(MCU TX)、TX 接 `P10_6`(MCU RX)、RTS 接 `P10_2`。

下行只有一条命令：

```text
<参数名> <值>\r\n
```

回 `ack:1.000` 表示写进去了，`ack:0.000` 表示被拒。

- 参数名不区分大小写，名和值之间用空格或 Tab，多个空格也行。
- 只有下面这 24 个名字放行，其余参数一律用车上的按键改。
- 波形不用命令切，跟着菜单页走。这里也没有任何发车或停车命令。

## 24 条命令

### Roll —— A/B 动量轮差动

| 命令 | 环 | 范围 |
|---|---|---|
| `r_rate_kp <值>` | 角速度环（增量式，1ms） | ±2000 |
| `r_rate_ki <值>` | 角速度环 | ±200 |
| `r_rate_kd <值>` | 角速度环 | ±500 |
| `r_angle_kp <值>` | 角度环（位置式，5ms） | ±2000 |
| `r_angle_ki <值>` | 角度环 | ±200 |
| `r_angle_kd <值>` | 角度环 | ±500 |
| `r_rcy_kp <值>` | 飞轮回收环（20ms） | ±50 |
| `r_rcy_ki <值>` | 飞轮回收环 | ±20 |
| `r_rcy_kd <值>` | 飞轮回收环，霍尔六步低速回传是阶梯值，保持 0 | ±50 |

```text
r_rate_ki 0.35
r_rate_kp 12.5
r_angle_kp 180
```

### Pitch —— C 行进轮

| 命令 | 环 | 范围 |
|---|---|---|
| `p_rate_kp <值>` | 角速度环（增量式，1ms） | ±500 |
| `p_rate_ki <值>` | 角速度环 | ±50 |
| `p_rate_kd <值>` | 角速度环 | ±500 |
| `p_angle_kp <值>` | 角度环（位置式，5ms） | ±2000 |
| `p_angle_ki <值>` | 角度环 | ±200 |
| `p_angle_kd <值>` | 角度环 | ±500 |
| `p_vel_kp <值>` | 速度环（20ms） | ±50 |
| `p_vel_ki <值>` | 速度环 | ±20 |
| `p_vel_kd <值>` | 速度环 | ±50 |

```text
p_rate_ki 0.02
p_rate_kp 1.5
p_vel_kp 0.008
```

### Yaw —— A/B 动量轮同向

| 命令 | 环 | 范围 |
|---|---|---|
| `y_rate_kp <值>` | 角速度内环（**位置式**，1ms） | ±200 |
| `y_rate_ki <值>` | 角速度内环 | ±50 |
| `y_rate_kd <值>` | 角速度内环 | ±200 |
| `y_angle_kp <值>` | 转向外环（位置式，5ms） | ±50 |
| `y_angle_ki <值>` | 转向外环 | ±50 |
| `y_angle_kd <值>` | 转向外环 | ±200 |

Yaw 内环是位置式，`kp` 才是主增益，别照抄 Roll/Pitch 的"先给 ki"。

```text
y_rate_kp 3
y_angle_kp 1.2
```

## 什么时候会回 ack:0.000

| 情况 | 例子 |
|---|---|
| 名字不在这 24 个里 | `cam_exposure 48`、`motor_dir_a 1` |
| 字段数不是 2 个 | `r_rate_kp`、`r_rate_kp 1 2` |
| 数值解析不完整 | `r_rate_kp 12.5abc`、`r_rate_kp --3`、`r_rate_kp .` |
| 数值是 NaN 或无穷 | `r_rate_kp nan`、`r_rate_kp 1e40` |
| 架空点动中 | Motor 页正在点动 A/B/C。PID 根本不在环里，改了没意义 |
| 单轴测试中改了别的轴 | 正在跑 Roll Rate 却发 `p_rate_kp` |
| 单轴测试中改了没启用的外环 | 只开了 `Rate` 却发 `r_angle_kp` |
| `START_DRIVE_ONLY` 发车中 | 只有 C 轮在跑，改 Roll/Yaw 增益看不出变化 |

**主菜单 `Balance` 运行期间 24 个全放行**，三轴都在环里，整定本来就要边跑边改。
越界仍然钳位，NaN/Inf 仍然拒绝。

空行不算错误，不回 ack。

越界但合法的有限值不会被拒，会钳到上表的范围再写进去，仍回 `ack:1.000`。想确认实际生效值就去菜单对应页看。

改完的值在内存里立刻生效，但**不会自动存 Flash**。要掉电保留，去对应的菜单页按 `Save`。

子页里的 `Save` 只存那一页的参数，`Params` 最底下的 `Save` 存全部。
串口改的是哪个轴的 PID，就去哪个轴的页面按 Save —— 在 Roll 页按 Save 存不进 Pitch 的增益。

## 上行波形

VOFA+ FireWater 文本帧，格式 `<tag>:v0,v1,...
`，默认约 20 ms 一帧。

| tag | 通道数 | 什么时候出 |
|---|---:|---|
| `att` | 3 | `Params → Attitude` 页按 `Test: ON` |
| `run` | 25 | 正式 Run 启动成功后自动开启；Run 结束后继续输出尾段，进入其他菜单页时关闭 |

`run:` 每帧带控制器毫秒时间戳。上位机或无线链路丢帧时，相邻 CH0 不再相差约 20，可据此识别，
不要把缺失样本误判成控制器停顿。

| CH | 内容 | 单位/说明 |
|---:|---|---|
| 0 | CPU0 控制器时间 | ms |
| 1 | Roll 实际角 | ° |
| 2 | Roll 实际目标 | °，已合成回收与压弯并经过总限幅 |
| 3 | Roll 角速度 | °/s |
| 4 | Roll 回收反馈 | RPM，A-B |
| 5 | Roll 回收输出 | ° |
| 6 | Roll 混控前输出 | duty |
| 7 | 压弯动态零点 | ° |
| 8 | 视觉目标横摆角速度 | °/s，斜率限制前 |
| 9 | 实际下发的目标横摆角速度 | °/s，斜率限制后 |
| 10 | 实际 Yaw 角速度 | °/s，IMU gyro_z |
| 11 | Yaw 混控前输出 | duty |
| 12 | Roll 优先后实际分配给 Yaw 的输出 | duty；与 CH11 不同表示被 Roll 余量裁剪 |
| 13 | A/B 共模转速 | RPM，(A+B)/2 |
| 14 | 滤波后方向像素误差 | pixel |
| 15 | BEV 横向误差 | 赛道半宽归一化 |
| 16 | 赛道航向误差 | ° |
| 17 | BEV 有符号曲率 | 归一化 |
| 18 | Run 规划速度 | m/s，含元素、质量和动量限制 |
| 19 | 速度斜坡输出 | m/s |
| 20 | C 轮实测速度 | m/s |
| 21 | Yaw 动量权限 | 0~1 |
| 22 | 视觉拟合质量 | 0~1 |
| 23 | 距最新视觉帧时间 | ms |
| 24 | 状态位 | 按下表解码 |

CH24 是无符号整数位图：

| 位 | 含义 |
|---:|---|
| 0 | 正式 Run 正在行驶 |
| 1 | 三轴 Balance 闭环仍在运行 |
| 2 | 当前赛道有效 |
| 3 | 本次 Run 已收到并应用视觉帧 |
| 4 | IPM 有效 |
| 5 | 转向处于 HOLD |
| 6 | 已锁存斑马线停车请求 |
| 8~11 | 当前元素编号 |
| 12~14 | `run_stop_t` 停车原因 |

为了诊断一次完整 Run，原始串口文本必须从发车前开始保存，到停车或失控后至少再保留 2 秒。
不要只截 VOFA 曲线截图；原始 `run:` 文本才能计算时延、飞轮动量增长率和丢帧。

## 收不到数据先查这里

1. 模块本身。没插好、没供电或者挂了，`P10_2` 上的 RTS 会一直被拉高，
   发送侧看到 RTS 高就整拍不发，一个字节也出不去。
2. 模块和电脑端的配对通道要一致，波特率两边都得是 115200。
   工程里 `WIRELESS_UART_AUTO_BAUD_RATE` 是关的，模块保持出厂波特率。
3. 当前菜单页和正在跑什么决定发不发波形。停在主菜单又没启动 `Balance` 就是 `VOFA_OFF`，什么都不发。
