# 调参命令（当前 cfg v1）

UART2 默认 115200、8N1。MCU TX P10_5、RX P10_6，模块 RTS P10_2。

本仓库当前源码不再支持旧文档的 `<参数名> <值>` / `ack:` 协议。新增配置命令如下，整行以 CRLF 结束：

```text
cfg:hello,1
cfg:schema,2
cfg:get,3,r_rate_kp
cfg:set,4,r_rate_kp,12.5
cfg:status,5
cfg:save,6,Roll
stop
```

`set` 成功返回 `rsp:4,ok,set,r_rate_kp,实际值,APPLIED`，钳位时为 `CLAMPED`。未收到应答不视为成功。
参数权威来源为 `g_param_table`，当前 72 项。范围、类型和权限由 Schema 与 stat 提供；运行时仅允许当前参与闭环的 PID，Jog 禁写，危险参数要求 STOP。RAM 修改不自动保存 Flash。

保存仅允许稳定停止，复用参数模块现有逐字回读验证。PC 危险参数另有高级解锁、差异预览和二次确认。

原 `speed:<相对转角>,<速度原始值>` 命令保留，两个输入均 −90～90，速度原始值除以 30 转为 m/s，仅车身按键启动 Remote 后有效。上位机不提供发车或遥控速度操作。
`stop` 任意模式均经 `control_stop()` 停机；不再接受没有行结束符的粘连命令。

完整配置帧、错误码、状态字段与安全条件见 [protocol.md](tools/unicycle_station/docs/protocol.md)。
当前 Roll rate 为位置式，Pitch rate 为增量式，Yaw rate 为位置式；不可直接照搬旧整定笔记的增益。

## 上行波形

VOFA+ FireWater 文本帧，格式 `<tag>:v0,v1,...
`，默认约 20 ms 一帧。

| tag | 通道数 | 什么时候出 |
|---|---:|---|
| `att` | 3 | `Params → Attitude` 页按 `Test: ON` |
| `run` | 25 | 正式 Run 启动成功后自动开启；Run 结束后继续输出尾段，进入其他菜单页时关闭 |

`run:` 每帧带控制器毫秒时间戳。上位机或无线链路丢帧时，相邻 CH0 不再相差约 20，可据此识别，
不要把缺失样本误判成控制器停顿。

| CH | 内容 | 单位/说明 |
|---:|---|---|
| 0 | CPU0 控制器时间 | ms |
| 1 | Roll 实际角 | ° |
| 2 | Roll 实际目标 | °，已合成回收与压弯并经过总限幅 |
| 3 | Roll 角速度 | °/s |
| 4 | Roll 回收反馈 | RPM，A-B |
| 5 | Roll 回收输出 | ° |
| 6 | Roll 混控前输出 | duty |
| 7 | 压弯动态零点 | ° |
| 8 | 视觉目标横摆角速度 | °/s，斜率限制前 |
| 9 | 实际下发的目标横摆角速度 | °/s，斜率限制后 |
| 10 | 实际 Yaw 角速度 | °/s，IMU gyro_z |
| 11 | Yaw 混控前输出 | duty |
| 12 | Roll 优先后实际分配给 Yaw 的输出 | duty；与 CH11 不同表示被 Roll 余量裁剪 |
| 13 | A/B 共模转速 | RPM，(A+B)/2 |
| 14 | 滤波后方向像素误差 | pixel |
| 15 | BEV 横向误差 | 赛道半宽归一化 |
| 16 | 赛道航向误差 | ° |
| 17 | BEV 有符号曲率 | 归一化 |
| 18 | Run 规划速度 | m/s，含元素、质量和动量限制 |
| 19 | 速度斜坡输出 | m/s |
| 20 | C 轮实测速度 | m/s |
| 21 | Yaw 动量权限 | 0~1 |
| 22 | 视觉拟合质量 | 0~1 |
| 23 | 距最新视觉帧时间 | ms |
| 24 | 状态位 | 按下表解码 |

CH24 是无符号整数位图：

| 位 | 含义 |
|---:|---|
| 0 | 正式 Run 正在行驶 |
| 1 | 三轴 Balance 闭环仍在运行 |
| 2 | 当前赛道有效 |
| 3 | 本次 Run 已收到并应用视觉帧 |
| 4 | IPM 有效 |
| 5 | 转向处于 HOLD |
| 6 | 已锁存斑马线停车请求 |
| 8~11 | 当前元素编号 |
| 12~14 | `run_stop_t` 停车原因 |

为了诊断一次完整 Run，原始串口文本必须从发车前开始保存，到停车或失控后至少再保留 2 秒。
不要只截 VOFA 曲线截图；原始 `run:` 文本才能计算时延、飞轮动量增长率和丢帧。

## 收不到数据先查这里

1. 模块本身。没插好、没供电或者挂了，`P10_2` 上的 RTS 会一直被拉高，
   发送侧看到 RTS 高就整拍不发，一个字节也出不去。
2. 模块和电脑端的配对通道要一致，波特率两边都得是 115200。
   工程里 `WIRELESS_UART_AUTO_BAUD_RATE` 是关的，模块保持出厂波特率。
3. 当前菜单页和正在跑什么决定发不发波形。旧固件可能处于 VOFA_OFF；本次扩展仍发 stat，cfg 握手后还提供低频 att。


### 上位机 task1 任务观测扩展（2026-09-07）

新版 `cfg:hello` 固件标识为 `tc264-cfg1-task1`，协议版本仍是 1。新增 `task:` 13 字段周期状态和 `taskevt:` 7 字段状态转换事件，保持原 att/run/stat 与下行指令兼容。用于上位机显示直道、左右弯、十字、环岛阶段等观测状态，并使用同步轮速/航向估算相对轨迹；不新增远程发车指令或改变控制参数。完整字段与运行频率见 [上位机协议](tools/unicycle_station/docs/protocol.md) 和 [调试增强说明](tools/unicycle_station/docs/debug-workflow.md)。本轮未执行 ADS 构建或烧录。
