# 调参命令

无线转串口模块，UART2 @115200。模块 RX 接 `P10_5`(MCU TX)、TX 接 `P10_6`(MCU RX)、RTS 接 `P10_2`。

下行只有一条命令：

```text
<参数名> <值>\r\n
```

回 `ack:1.000` 表示写进去了，`ack:0.000` 表示被拒。

- 参数名不区分大小写，名和值之间用空格或 Tab，多个空格也行。
- 只有下面这 24 个名字放行，其余参数一律用车上的按键改。
- 波形不用命令切，跟着菜单页走。这里也没有任何发车或停车命令。

## 24 条命令

### Roll —— A/B 动量轮差动

| 命令 | 环 | 范围 |
|---|---|---|
| `r_rate_kp <值>` | 角速度环（增量式，1ms） | ±2000 |
| `r_rate_ki <值>` | 角速度环 | ±200 |
| `r_rate_kd <值>` | 角速度环 | ±500 |
| `r_angle_kp <值>` | 角度环（位置式，5ms） | ±2000 |
| `r_angle_ki <值>` | 角度环 | ±200 |
| `r_angle_kd <值>` | 角度环 | ±500 |
| `r_rcy_kp <值>` | 飞轮回收环（20ms） | ±50 |
| `r_rcy_ki <值>` | 飞轮回收环 | ±20 |
| `r_rcy_kd <值>` | 飞轮回收环，霍尔六步低速回传是阶梯值，保持 0 | ±50 |

```text
r_rate_ki 0.35
r_rate_kp 12.5
r_angle_kp 180
```

### Pitch —— C 行进轮

| 命令 | 环 | 范围 |
|---|---|---|
| `p_rate_kp <值>` | 角速度环（增量式，1ms） | ±500 |
| `p_rate_ki <值>` | 角速度环 | ±50 |
| `p_rate_kd <值>` | 角速度环 | ±500 |
| `p_angle_kp <值>` | 角度环（位置式，5ms） | ±2000 |
| `p_angle_ki <值>` | 角度环 | ±200 |
| `p_angle_kd <值>` | 角度环 | ±500 |
| `p_vel_kp <值>` | 速度环（20ms） | ±50 |
| `p_vel_ki <值>` | 速度环 | ±20 |
| `p_vel_kd <值>` | 速度环 | ±50 |

```text
p_rate_ki 0.02
p_rate_kp 1.5
p_vel_kp 0.008
```

### Yaw —— A/B 动量轮同向

| 命令 | 环 | 范围 |
|---|---|---|
| `y_rate_kp <值>` | 角速度内环（增量式，1ms） | ±200 |
| `y_rate_ki <值>` | 角速度内环 | ±50 |
| `y_rate_kd <值>` | 角速度内环 | ±200 |
| `y_angle_kp <值>` | 转向外环（位置式，5ms） | ±50 |
| `y_angle_ki <值>` | 转向外环 | ±50 |
| `y_angle_kd <值>` | 转向外环 | ±200 |

```text
y_rate_ki 0.05
y_rate_kp 3
y_angle_kp 1.2
```

## 什么时候会回 ack:0.000

| 情况 | 例子 |
|---|---|
| 名字不在这 24 个里 | `cam_exposure 48`、`motor_dir_a 1` |
| 字段数不是 2 个 | `r_rate_kp`、`r_rate_kp 1 2` |
| 数值解析不完整 | `r_rate_kp 12.5abc`、`r_rate_kp --3`、`r_rate_kp .` |
| 数值是 NaN 或无穷 | `r_rate_kp nan`、`r_rate_kp 1e40` |
| 发车中 | `start_flag != START_STOP` |
| 架空点动中 | Motor 页正在点动 A/B/C |
| 闭环测试中改了别的轴 | 正在跑 Roll Rate 却发 `p_rate_kp` |
| 闭环测试中改了没启用的外环 | 只开了 `Rate` 却发 `r_angle_kp` |

空行不算错误，不回 ack。

越界但合法的有限值不会被拒，会钳到上表的范围再写进去，仍回 `ack:1.000`。想确认实际生效值就去菜单对应页看。

改完的值在内存里立刻生效，但**不会自动存 Flash**。要掉电保留，去 `Params → Save`。

## 上行波形

VOFA+ FireWater 文本帧，格式 `<tag>:v0,v1,...
`，默认约 20 ms 一帧。

**波形不用选，在哪个页面开的测试就是哪个波形，退出即关。**

| tag | 通道数 | 什么时候出 |
|---|---|---|
| `att` | 3 | `Params → Attitude` 页按 `Test: ON` |
| `roll` | 11 | `Params → Roll` 页启动任一分环 Test |
| `pit` | 7 | `Params → Pitch` 页启动任一分环 Test |
| `yaw` | 6 | `Params → Yaw` 页启动任一分环 Test |
| `mot` | 5 | `Params → Motor` 页点动 |
| `trk` | 13 | 进 `Params → Camera` 或 `Params → Element` 页 |

其余页面不发波形。同一个 tag 的通道数恒定，换 tag 之前先在上位机清一次曲线。

通道含义见 `控制调试指南.md` 第 6 节。

## 收不到数据先查这里

1. 模块本身。没插好、没供电或者挂了，`P10_2` 上的 RTS 会一直被拉高，
   发送侧看到 RTS 高就整拍不发，一个字节也出不去。
2. 模块和电脑端的配对通道要一致，波特率两边都得是 115200。
   工程里 `WIRELESS_UART_AUTO_BAUD_RATE` 是关的，模块保持出厂波特率。
3. 当前菜单页决定发不发波形。停在主菜单是 `VOFA_OFF`，什么都不发。
