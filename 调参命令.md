# 调参命令（cfg v1 / task1）

核对日期：2026-09-11（含上位机 0.5.1 对应的在线调参和 Run Test 波形扩展）。依据 code/vofa.c、vofa_task.inc 和 board_config.h；参数完整默认值和范围见 [参数参考](参数参考.md)。

UART2 默认 115200、8N1。MCU TX P10_5、RX P10_6，模块 RTS P10_2。

本仓库当前源码不再支持旧文档的 `<参数名> <值>` / `ack:` 协议。新增配置命令如下，整行以 CRLF 结束：

```text
cfg:hello,1
cfg:schema,2
cfg:get,3,r_rate_kp
cfg:set,4,r_rate_kp,12.5
cfg:status,5
cfg:save,6,Roll
stop
```

`set` 成功返回 `rsp:4,ok,set,r_rate_kp,实际值,APPLIED`，钳位时为 `CLAMPED`。未收到应答不视为成功。
参数权威来源为 `g_param_table`，当前 74 项；编译默认 PID 已非全零，Flash 可覆盖。范围、类型和权限由 Schema 与 stat 提供；运行时允许当前参与闭环的 PID；0.5.1 在 Balance/Run/Remote 另外开放明确的速度、转向和压弯白名单（Schema bit4 与 stat bit24 双重确认），Jog 禁写，危险参数要求 STOP。RAM 修改不自动保存 Flash。

保存仅允许稳定停止，复用参数模块现有逐字回读验证。PC 危险参数另有高级解锁、差异预览和二次确认。

原 `speed:<相对转角>,<速度原始值>` 命令保留，两个输入均 −90～90，速度原始值除以 40 转为 m/s，超出 ±1.50 m/s 会拒绝而非钳位（因此有效速度原始值实际为 ±60），仅车身按键启动 Remote 后有效。上位机 0.5.0 提供按住式前进、后退和转向，但仍由车身确认进入 Remote。新版 `cfg:remote,<seq>` 只读查询 active/最近转角/速度/命令年龄，供上位机确认遥控许可；具体步骤见 [遥控说明](tools/unicycle_station/docs/experience-remote.md)。
`stop` 任意模式均经 `control_stop()` 停机；不再接受没有行结束符的粘连命令。

完整配置帧、错误码、状态字段与安全条件见 [protocol.md](tools/unicycle_station/docs/protocol.md)。
当前 Roll rate 为位置式，Pitch rate 为增量式，Yaw rate 为位置式；不可直接照搬旧整定笔记的增益。

## 上行波形

VOFA+ FireWater 文本帧，格式 `<tag>:v0,v1,...\n`，普通 att/run 默认约 20 ms 一帧。

| tag | 通道数 | 什么时候出 |
|---|---:|---|
| `att` | 3 | Attitude 页 Test 开启时约 50 Hz；hello 后非该模式另有约 10 Hz 辅助姿态 |
| `run` | 25 | 正式 Run 自动开启；最新版固件 hello 后，单轴 Test 或 START_BALANCE（含 Run Test 的 Balance/Remote）也自动发送，默认约 50 Hz。标签不代表正式 Run，任务与轨迹仍按 CH24 bit0 判定 |
| `stat` | 15 | 约 10 Hz，不要求 hello 或发车 |
| `par / rsp` | 变长 | cfg Schema / 带序号应答 |
| `task` | 13 | hello 后约 5 Hz，独立于普通 VOFA 模式 |
| `taskevt` | 7 | hello 后任务转换事件，最多 50 条/秒出队 |

`run:` 每帧带控制器毫秒时间戳。上位机或无线链路丢帧时，相邻 CH0 不再相差约 20，可据此识别，
不要把缺失样本误判成控制器停顿。

| CH | 内容 | 单位/说明 |
|---:|---|---|
| 0 | CPU0 控制器时间 | ms |
| 1 | Roll 估计角 | °，IMU 融合结果，动态压弯需外部参照验证 |
| 2 | Roll 实际目标 | °，已合成回收与压弯并经过总限幅 |
| 3 | Roll 角速度 | °/s |
| 4 | Roll 回收反馈 | RPM，A-B |
| 5 | Roll 回收输出 | ° |
| 6 | Roll 混控前输出 | duty |
| 7 | 压弯动态零点 | ° |
| 8 | 视觉目标横摆角速度 | °/s，斜率限制前 |
| 9 | 航向目标积分使用的横摆角速度命令 | °/s，斜率限制后；不是 Yaw 角速度内环的实际设定值 |
| 10 | 实际 Yaw 角速度 | °/s，IMU gyro_z |
| 11 | Yaw 混控前输出 | duty |
| 12 | Roll 优先后实际分配给 Yaw 的输出 | duty；与 CH11 不同表示被 Roll 余量裁剪 |
| 13 | A/B 共模转速 | RPM，(A+B)/2 |
| 14 | 滤波后方向像素误差 | pixel |
| 15 | BEV 横向误差 | 赛道半宽归一化 |
| 16 | 赛道航向误差 | ° |
| 17 | BEV 有符号曲率 | 归一化 |
| 18 | Run 规划速度 | m/s，含元素、质量和动量限制 |
| 19 | 速度斜坡输出 | m/s |
| 20 | C 轮实测速度 | m/s |
| 21 | Yaw 动量权限 | 0~1 |
| 22 | 视觉拟合质量 | 0~1 |
| 23 | 距最新视觉帧时间 | ms |
| 24 | 状态位 | 按下表解码 |

CH24 是无符号整数位图：

| 位 | 含义 |
|---:|---|
| 0 | 正式 Run 正在行驶 |
| 1 | 三轴 Balance 闭环仍在运行 |
| 2 | 当前赛道有效 |
| 3 | 本次 Run 已收到并应用视觉帧 |
| 4 | IPM 有效 |
| 5 | 转向处于 HOLD |
| 6 | 已锁存斑马线停车请求 |
| 8~11 | 当前元素编号 |
| 12~14 | `run_stop_t` 停车原因 |

为了诊断一次完整 Run，原始串口文本必须从发车前开始保存，到停车或失控后至少再保留 2 秒。
不要只截 VOFA 曲线截图；原始 `run:` 文本才能计算时延、飞轮动量增长率和丢帧。

## 收不到数据先查这里

1. 模块本身。没插好、没供电或者挂了，`P10_2` 上的 RTS 会一直被拉高，
   发送侧看到 RTS 高就整拍不发，一个字节也出不去。
2. 模块和电脑端的配对通道要一致，波特率两边都得是 115200。
   工程里 `WIRELESS_UART_AUTO_BAUD_RATE` 是关的，模块保持出厂波特率。
3. 最新版固件完成 cfg 握手后，Run Test 的 Balance/Remote 和单轴 Test 都会自动发送 run 诊断波形；无需先开启正式 Run。停车时可用 Attitude Test 观察姿态。旧固件可能仍依赖菜单 VOFA 模式，需要烧录对应新版固件。


### 上位机 task1 任务观测扩展（2026-09-07）

新版 `cfg:hello` 固件标识为 `tc264-cfg1-task1`，协议版本仍是 1。新增 `task:` 13 字段周期状态和 `taskevt:` 7 字段状态转换事件，保持原 att/run/stat 与下行指令兼容。用于上位机显示直道、左右弯、十字、环岛阶段等观测状态，并使用同步轮速/航向估算相对轨迹；不新增远程发车指令或改变控制参数。完整字段与运行频率见 [上位机协议](tools/unicycle_station/docs/protocol.md) 和 [调试增强说明](tools/unicycle_station/docs/debug-workflow.md)。本次文档核对未执行 ADS 构建或烧录。

## 新压弯参数（2026-09-11）

当前 Lean 组为 `lean_turn_kp`、`lean_speed_kp`、`lean_slew_dps`、`lean_max_angle`。旧 `lean_roll_kp` 已移除，旧日志中的值不能直接换名复用。默认值、范围与实车对照见 [压弯控制说明](压弯控制说明.md)；先刷新 MCU Schema，再按现有 RAM 应用/停车保存流程调节。
