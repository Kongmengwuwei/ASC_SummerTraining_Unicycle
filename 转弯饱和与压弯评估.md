# 转弯饱和与压弯评估

> 历史分析：本文记录 2026-09-09 的旧压弯算法及扩展上限改动。2026-09-11 已替换目标生成公式和增益名称；当前操作请使用 [压弯控制说明](压弯控制说明.md)。下面原始数值与验证记录保留用于解释旧日志。

核对日期：2026-09-09。依据为当前工作区 `code/control.c`、`board_config.h`、`attitude.c`、`param.c` 及遥测实现。用户确认没有实车日志；本文的数值是代码计算示例，不是实车测量。Flash 可能覆盖编译默认值，调参前须保存实际 Schema/参数快照。

## 1. 结论和本次修改

当前程序确实实现了压弯，并送入 Roll 目标；但“启用了压弯”“车身向弯内倾斜”“压弯减少了飞轮持续加速”是三件不同的事。最优先核查压弯触发条件、压弯依据、回收抵消和单轮转速余量，不能凭上位机 CH7 非零就判断有效。

本次仅扩大可调范围并同步相关文档、skill、上位机 Profile 和示例工作区，没有改控制律或写入实车参数：

| 项目 | 原范围 | 新范围 | 默认值 |
|---|---|---|---|
| `lean_roll_kp` / Lean Kp | 0～20 | 0～100 | 2.5，不变 |
| `lean_max_angle` / Lean Max | 0～8° | 0～10° | 5°，不变 |
| 回收与压弯合成 Roll 目标偏移 | ±10° | ±10°，不变 | 硬限幅 |

`DIRECTION_ROLL_KP_MAX` 统一增益上限；`DIRECTION_LEAN_LIMIT` 同时用于参数表与控制函数的角度上限。菜单和 MCU cfg Schema 使用参数表。参数名称、数量、Flash 布局不变。现有有效 Flash 参数不会因扩大范围自动增大；编译烧录后重新读取 MCU Schema，才能在连接实车的上位机看到新范围。现有打包 EXE 和用户另存的工作区可能仍含旧快照。

10° 是沿用现有 Roll 总目标边界作出的范围选择，不是证明车辆能稳定压到 10°。若回收同时占用目标余量，实际净目标会更小或反向。新增范围适合逐步测试，不建议直接把 Kp 设为 100。没有调整飞轮超速阈值、保护角度或输出限幅。

## 2. 源码已确认的薄弱点

### 2.1 压弯依据航向跟踪误差，而不是持续转弯需求

`cascade_run()` 把航向目标与实际航向的差限制到 ±35°，除以 200，送入每 5 ms 执行的 `lean_offset_update()`。后者在正式 Run 且原始加权视觉偏差绝对值 >1000 时计算：

```text
lean_raw[deg] = heading_error[deg] × C_counts_20ms × lean_roll_kp / 20000
C_counts_20ms ≈ speed[m/s] × odom_counts_per_m × 0.020
```

随后受 Lean Max / 10° 硬上限及每 5 ms 0.08° 的变化量限制。这里的 `heading_error` 是车体航向目标跟踪误差，不是 CH16 的视觉赛道航向误差。

使用编译默认标定 11690 count/m、速度 1.2 m/s、Kp=2.5，即使航向跟踪误差达到 35°，公式也只给约 **1.23°**；这时把 Lean Max 从 5° 增到 10°没有作用，因为原本就未触顶。更常见的小航向误差会使压弯更小。

作为量级示例，若 Yaw 角度环仅有默认 P=14，稳态理想跟踪 60°/s 时，航向误差约为 60/14=4.29°，上述压弯约 0.15°。该估算忽略动态、耦合、饱和和实际参数，只说明此公式可能使“转向跟得越好，压弯反而越小”。不能把该值当成当前车身的真实倾角。

对平地、无明显侧滑、稳定圆周运动的理想模型，所需内倾量级为：

```text
phi_eq = atan(v * yaw_rate_rad_s / g) = atan(v² / (g * R))
```

例如 v=1.2 m/s、转弯率=60°/s，约需 7.3°。这是力矩平衡参考，不是完整双飞轮独轮车模型；符号要按实车轴向校验，gyro_z 为机体系角速度，压弯较大时不能直接当作精确地面航向变化率。[伊利诺伊大学：转弯内倾的力矩平衡](https://van.physics.illinois.edu/ask/listing/14948)

**建议：** 后续将压弯主体改为有单位的速度×转弯率前馈，再叠加有限的小修正。转弯率指令可作预判，实测转弯率可作校正；评估实际转向跟不上时的过度内倾。当前视觉曲率是归一化量，不能直接当 1/m 代入物理公式。此控制律改造尚未实施。

### 2.2 视觉误差门控可能在稳定弯中关闭压弯

门控只看 `abs(g_vision_direction_camera)>1000`。若车辆已贴合曲线中心，横向偏差变小，但视觉曲率或实际转弯率仍非零，压弯目标会逐渐回到零。门限附近也可能反复切换。CH14 是滤波后像素误差，不是门控使用的原始加权值，因此现有日志不能精确还原开关瞬间。

**建议：** 以有效循迹状态、速度、转弯率/曲率需求共同判定，加入迟滞及平滑退出。需要覆盖低速、HOLD、失帧和停车，避免消除门控后直行漂移引发误压弯。

### 2.3 回收环可能把压弯抵消，入弯响应也偏慢

当前：

```text
net_roll_target = roll_zero + clamp(LEAN_DIR * lean_offset - recovery_output, -10°, +10°)
recovery_feedback = RPM_A - RPM_B
```

默认回收 P=0.0017，I=D=0；两轮差速为 3000 RPM 时，回收输出约 5.1°，能大于前述 1.23° 压弯指令。两者同号时相减，异号时相加；是否有害必须结合弯向、轮速和实际 Roll 判断。回收本身有必要，不能为了看到压弯就直接关闭。

压弯变化速度为 16°/s，从 0 到 5°最快约 0.31 s，到 10°约 0.63 s；左右弯反向时更慢。增大上限不会加快响应。

**建议：** 将转弯平衡目标和飞轮回收偏置显式分开，记录限幅前后各项；结合动量余量给回收分配幅值和变化速度。预瞄压弯必须与转向响应时序配合，不宜仅提高斜率。

### 2.4 共模降权不能覆盖单轮先饱和，也不等于主动卸载

当前用 M=(A+B)/2 的绝对值，在 2500～6000 RPM 间逐渐降低转向和 Run 速度，最低保留 25%。回收使用 D=A−B。仅看 M 会漏掉 Roll 差速占用的单轮余量：

```text
A = M + D/2
B = M - D/2
max(abs(A), abs(B)) = abs(M) + abs(D)/2
RPM_margin = live_fly_speed_limit - abs(M) - abs(D)/2
```

例：M=6000、D=3000 时，单轮达到 7500 RPM，已经超过默认 7000 RPM 阈值。实际超速保护仍逐轮检查并全停电机，因此应区分“轮速上升后控制余量耗尽”和“保护先触发、断开平衡后倒下”。检查 `stat` 中的控制状态/保护原因，不能只看 Run 停车位。

上式是同一时刻、有符号逻辑 RPM 的代数关系；既有 CH4/13 可供近似诊断，应注意驱动反馈新鲜度和采样一致性。没有惯量、轮轴布置和单位换算时，M/D 都只是转速代理量，不是已标定的角动量。

减少转向请求可以减慢动量积累，但不能保证飞轮回到可用工作区。飞轮容量有限，真正卸载需要系统与外界交换角动量；独轮车应结合轮地作用、车体姿态和路径可行性设计，不能直接套用航天器执行机构方案。[NASA：姿态控制与飞轮卸载原理](https://science.nasa.gov/learn/basics-of-space-flight/chapter11-2/)

**建议：** 用两只轮各自的转速/加速趋势、输出余量、反馈时效共同分配 Roll/Yaw 权限，提前降低弯中速度；评估带滞环的限制与卸载策略。保留 Roll 优先和现有硬保护。不能只提高转速保护上限。

### 2.5 Yaw 裁剪后，上游航向目标仍继续积累

当前 Yaw 输出只使用 Roll 剩余的 A/B duty 余量。CH11 为请求值，CH12 为裁剪后分配值；两者长期不一致说明执行器无法兑现请求。上游仍把转弯率积分为航向目标，只限制到实际航向前后 35°。

默认 Yaw 角度 P=14 时，35° 误差可形成 490°/s 的角速度环目标。它不是规划层的 120°/s 上限，所以 CH9 也不是实际 Yaw 内环设定值。仅降低规划层速度不会立即释放已有航向超前量。即使 Yaw PID 的 I=0，也仍存在这个上游目标累积问题。

**建议：** 用实际分配权限限制可实现转弯率，并给航向目标积分加入条件积分/回算或跟踪，检查松开限制后的突发转向。不要只继续加大 Yaw P。

### 2.6 编译默认弯速与直道速度相同

直道、弯道、十字、环岛、坡道的默认速度均为 1.2 m/s。质量和动量限制仍可降速，但单靠直道/弯道插值不会在正常高质量弯道自动变慢。

**建议：** 先在已知安全的低速下建立弯速与实际半径、倾角和转速余量的对照，单独降低弯道速度并验证。后续可按可用内倾角计算曲率限速：v_max≈sqrt(g*tan(phi_available)/abs(kappa))，另叠加轮速余量、摩擦和减速距离约束。仅可用于已标定为 1/m 的曲率；不能把 CH17 直接代入。

## 3. 如何证明“真有压弯”和“压弯有效”

按下面四层分别给证据，任何一层失败都应先定位，不能直接继续增大 Kp。

| 层次 | 观察方法 | 判定含义 |
|---|---|---|
| 1. 发出压弯指令 | Run CH7 在弯中持续非零，并检查速度、门控和限幅 | 只证明软件输出了压弯偏移 |
| 2. 形成净倾斜目标 | 对比 CH2−实际 roll_zero、CH7、CH5；计算 `LEAN_DIR*CH7−CH5` 与 ±10° 限幅 | 判断回收是否抵消、符号是否正确、目标是否撞限 |
| 3. 车体真正向弯内倾 | CH1 跟踪 CH2，同时用有地面竖直参照的外部视频观察车架 | 姿态估计与物理倾角相符才有说服力 |
| 4. 减轻飞轮负担 | 相同路线、实际速度、入弯初始轮速下，比较峰值单轮 RPM、余量、M/D 漂移、Yaw 裁剪与 Roll 跟踪 | 车仍完成同一转弯且余量改善，才支持压弯有效 |

外部视频尽量使观察方向接近车辆瞬时前后方向；透视、镜头倾斜和车体转向都会影响二维倾角。可先在已知静态倾角下标定车架标记与 IMU，再用两个视角核验。要看车架相对地面竖直线朝弯内倾斜，而非屏幕模型是否倾斜。曲线方向、左右转和传感器符号须实车核实。

### 为什么 IMU Roll 不能单独作真值

当前 `attitude.c` 的加速度模长门控为约 0.75g～1.25g，并按模长偏差降低 Mahony 加速度校正权重；未看到对持续转弯平动加速度的显式补偿。横向加速度会改变加速度计测到的合力方向，而模长变化可能很小。例如横向约 0.128g，合力模长仅约 1.008g，仍能通过门控。持续圆周运动中，校正可能把姿态拉向“表观重力”的方向。

这是需要验证的估计器风险，不是已证实本车 Roll 错误。先记录原始加速度、陀螺仪、校正权重/误差和外部倾角，再评估利用运动模型补偿或自适应权重；简单关闭加速度校正会带来漂移，不能当最终方案。[Analog Devices AN-1057：用加速度计测倾角的前提与动态加速度误差](https://www.analog.com/en/resources/app-notes/an-1057.html)

### 现有日志即可计算的指标

使用 `run:` CH0 的控制器时间，记录原始文本与实际参数快照；从发车前保存到停车后至少 2 秒，保留 `stat/task/taskevt`。现有通道说明见 [调参命令](调参命令.md)。

- 入弯、稳态弯、出弯分别统计，检查 CH23 视觉年龄和 CH24 状态位，剔除掉帧严重或模式不同的比较段。
- 净目标偏移 = CH2−roll_zero；估计 Roll 跟踪误差 = CH1−CH2。另统计 CH7/净目标首次显著变化相对实际转弯率 CH10 的延迟。
- M=CH13，D=CH4，推算单轮峰值与余量。用约 100～200 ms 窗口估计 RPM 变化率，避免逐帧求导放大反馈量化与重复样本。
- Yaw 裁剪量 = CH11−CH12；在超过实际噪声/量化阈值时统计占比与持续时长。阈值应由静止/直行基线确定。
- 对比 CH18/19/20，保证“更不易饱和”并非仅来自速度更低；同时比较实际转弯完成情况，避免把少转、冲出弯道当改善。
- 同样保留保护发生前的波形和 `stat` 原因；不要只比较倒下前总时间。

现有遥测缺少精确门控原始量、航向超前误差、实际 Yaw 内环目标、加速度校正状态，以及驱动采样时间。下一轮可优先补这些诊断量，使用可选低频诊断或主机派生字段，先计算 UART2 115200 带宽预算，不能无限追加高频文本。

### 建议的对照试验顺序

1. 支撑/限位条件下确认 Roll、Yaw、A/B RPM、C 速度和 `LEAN_DIR` 的符号；静态已知倾角核对 IMU。压弯属于 Run，独立 Balance 没有这条常规压弯输入，不能以原地 Balance 没有 CH7 判断故障。
2. 在已验证安全的低速和较大固定弯半径，分别记录左转、右转与直行基线。保存电池状态、载荷、轮胎接触、初始 A/B 转速和全参数。
3. 保持 Lean Max=5°，逐步调整 Kp，每次只改一个量；先看 CH7 是否本来过小，再看净目标和实际内倾。仅当 CH7 稳定碰到现有上限、且目标/实际姿态仍可控，才逐步放宽 Lean Max。
4. 每个条件重复至少三次并交错顺序，避免电池和初始轮速偏差；支撑状态会改变外力矩，因此台架用于方向/响应检查，性能结论仍需可比的受控行驶。Kp=0 的对照仅在支撑或已验证安全工况下使用。
5. 预先设定轮速余量、Roll 偏差和视觉丢失的中止条件，在硬保护前结束；不以故意转到倒下作为测试目标。已有过饱和记录的速度不直接作为增益试验起点。

## 4. 更广泛的优化优先级

| 优先级 | 建议 | 验收证据 |
|---|---|---|
| P0 | 建立真实日志与保护前记录；显示单轮 RPM 余量、净压弯和 Yaw 裁剪 | 能区分超速全停、输出裁剪、姿态估计偏差和丢线 |
| P0 | 校验轴向/轮速符号、机械零点、轮地接触与电池状态；标定 C 速度 | 左右转行为可解释，物理倾角与估计一致 |
| P1 | 按实际转弯需求生成压弯，修正误差门控，协调回收与压弯时序 | 同路线同速度，实际内倾持续、飞轮余量改善 |
| P1 | 按单轮余量分配 Roll/Yaw；抑制不可实现的航向目标累积 | 持续裁剪减少，退出限制无突发转向 |
| P1 | 弯道提前减速并考虑减速距离、视觉时延、飞轮余量 | 入弯前速度达标，仍完成要求路径 |
| P2 | 验证动态加速度下姿态估计，增加反馈年龄诊断 | 外部倾角误差降低，掉包/旧反馈可识别 |
| P2 | 测量飞轮惯量、可用转速/扭矩曲线、摩擦与重心；建立简化耦合模型 | 区分软件可改与机械容量不足，参数有单位依据 |

扩大飞轮惯量/容量只能在确认硬件瓶颈后评估，它会影响质量、响应和可用扭矩，不能替代持续外力矩下的正确压弯与卸载策略。本次没有实施以上控制律或硬件改造。

## 5. 验证记录

- 主机回归：`test_lean_limits.py` 与 `test_firmware_source.py` 共 5 项通过。包括实际压弯函数提取编译、默认工况结果不变、新 10° 范围可达、正负硬限幅、门控/退出斜率，以及 Profile/示例工作区与 MCU 参数表一致。
- 主机检查隔离了电机反馈与参数依赖，不覆盖完整闭环、真实传感器、TASKING 目标行为或机械稳定性。
- ADS/TASKING：已尝试 ADS 1.10.10 的 headless Build Project，临时工作区位于工程外。排除启动参数和导入 URI 问题后，Project Booster 依赖 CDT UI，最终报 `Workbench has not been created yet` / `Errors running builder Project Booster`，没有取得完整目标编译链接成功结果。需在 ADS 图形工作台中 Build Project 验证；没有手改生成规则或绕过构建器。原始本地日志位于 `tools/unicycle_station/docs/lean-limits-ads-build.log`。
- `git diff --check` 与 skill 格式校验通过；未烧录，未进行台架或实车测试，没有实车日志可验证倒车原因。