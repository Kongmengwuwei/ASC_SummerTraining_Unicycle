# 菜单指南

屏幕上每一页、每一个参数是什么意思、怎么调。

整定步骤见 `控制调试指南.md`、`视觉调试指南.md`，串口调参见 `调参命令.md`。

## 按键

四个键：**上 / 下 / 确认 / 返回**。

| 场景 | 上 | 下 | 确认 | 返回 |
|---|---|---|---|---|
| 列表里 | 光标上移 | 光标下移 | 进入 / 执行 | 上一页 |
| 参数行按了确认（编辑态，名字前是 `*`） | 加一个步长 | 减一个步长 | 退出编辑 | 退出编辑 |
| Image 页 | 曝光 +2 | 曝光 -2 | 切显示模式 | 回主菜单 |

长按上下键连发，间隔 120 ms。

**改完的值只在内存里，要掉电保留必须按 Save。**
子页里的 `Save` 只存当前这一页，`Params` 最底下的 `Save` 存全部，见本文末尾。

编辑态下按返回，如果当前页开着 Test 会一并停掉。

## 主菜单

```text
> Params
  Image
  Balance: OFF

IMU  READY
BLDC UART OK
CAM  READY
```

`Balance` 是三轴同时闭环的入口，八个环全在环里，原地平衡，速度目标恒 0，视觉不参与。
每一轴单独调好之后才用它，调法见 `控制调试指南.md` 第 10 节。

- **连按两次才启动**：第一次显示 `PRESS AGAIN TO BALANCE`，3 秒内再按一次才真的松飞轮刹车。
  光标移开就撤销确认态。
- 跑起来那一行变成 `Balance: ON`，**再按一次是停**，返回键在任何页面都能立刻停。
- **翻页不停**，`bal` 波形也不断，方便一边看波形一边在别的页面看实时数据。
- 起步时车必须已经扶到保护角以内，否则拒绝启动并报 `ROLL OVER PROTECT` / `PITCH OVER PROTECT`。
- 运行期间**串口 24 个 PID 全放行**，边跑边改；菜单里的参数编辑和 Save/Reset 仍然被挡住。
- 被安全闸停掉时状态行直接显示原因：`FLYWHEEL OVERSPEED`、`BLDC LINK LOST`、
  `IMU LINK LOST`、`ATTITUDE DIVERGED`、`ROLL OVER PROTECT` 等。

三行状态是实时的，100 ms 刷一次：

| 行 | 正常 | 不正常时的含义 |
|---|---|---|
| IMU | `READY` | `INIT FAILED` 查 SPI0；`LINK LOST` 读数停更；`CALIB MOVED` 上电标定期间车动过，必须断电静置重来；`CONVERGING` 等就行；`ATT DIVERGED` 四元数发散 |
| BLDC | `UART OK` | `LINK LOST` UART3 断链，查 `P15_6/P15_7`。只挡 Roll/Yaw，Pitch 不受影响 |
| CAM | `READY` | `NOT INITIALIZED` 还没进过 Camera/Element/Image 页；`STARTING` 等；`INIT FAILED` 查 MT9V03X 接线；`WAITING FRAME` / `FRAME LOST` 有初始化但没帧 |

## Image

看图像用的，**这一页不发波形**。

状态栏：

```text
BIN+L  E  48  T118  V1  NONE  F 50
```

| 字段 | 含义 |
|---|---|
| `BIN+L` / `BIN` / `GRAY` | 显示模式，确认键切换 |
| `E` | 当前曝光 |
| `T` | 本帧大津阈值 |
| `V` | `Track_Valid`，1=本帧循迹有效 |
| `NONE` | 识别到的元素名 |
| `F` | CPU1 出帧率，跟本页刷屏速度无关 |

三种模式：`BIN+L` 二值图加边线（默认，调线用这个）、`BIN` 纯二值（调曝光和阈值用）、`GRAY` 原始灰度（查摄像头本身用）。

横屏方向是站在**车尾侧**看。停帧时状态栏每 500 ms 还会刷一次，所以 `F` 会掉到 0，不会冻住。

---

# Params

```text
> Attitude    姿态实时角
  Roll        横滚串级 PID
  Pitch       俯仰串级 PID
  Yaw         航向串级 PID
  Camera      循迹参数
  Element     元素开关与阈值
  Motor       电机极性与点动
  Zero        机械零点与保护角
  Save        写 Flash
  Reset       擦除并回默认值
  Back
```

## Attitude

没有参数，只看数。

```text
> Test: ON
  Back

Roll        1.2 deg
Pitch      -0.3 deg
Yaw        12.7 deg

ATTITUDE READY
```

确认键开关 `att` 波形（3 通道：Roll、Pitch、Yaw）。

`Yaw` 是连续角，不做 ±180° 包装，也不是绝对航向，六轴 IMU 长期会漂。

出 `CALIB MOVED / REBOOT` 就是上电静止标定期间车动过，零偏不可信，必须断电静置重新上电。

## Roll / Pitch / Yaw

三页结构一样：Roll 十二行参数、Pitch 九行、Yaw 六行，后面跟动作行。

```text
> Rate Kp        0.00
  Rate Ki       0.000
  Rate Kd        0.00
  Angle Kp       0.00
  Angle Ki      0.000
  Angle Kd       0.00
  Speed Kp      0.000
  Speed Ki      0.000
  Speed Kd      0.000
  Ang Lim          250
  Rcy Lim         3.0
  Rcy Tau         2.0
  Start Rate Test
  Start Angle Test
  Start Speed Test
  Save
  Back
```

`Speed` 那三行在 Roll 页是**飞轮回收环**（`r_rcy_*`），在 Pitch 页是**速度环**（`p_vel_*`）。Yaw 没有。
上面是 Roll 页的样子，Pitch 页的 `Speed` 三行显示四位小数（`0.0000`）。

**三页 24 个增益里只有 8 个要调**（Roll 3 个、Pitch 3 个、Yaw 2 个），
带"保持 0"的那些从头到尾不用动，理由见 `控制调试指南.md` 第 3 节。

| 行 | Roll | Pitch | Yaw | 干什么 |
|---|---|---|---|---|
| Rate Kp | `r_rate_kp` 步长 1.0 | `p_rate_kp` 步长 0.5 | `y_rate_kp` 步长 0.5 | **Roll/Yaw 是位置式，kp 是主增益**；Pitch 是增量式，kp 作用是阻尼 |
| Rate Ki | **保持 0** | `p_rate_ki` 步长 0.01 | **保持 0** | 只有增量式内环用得上，Pitch **先调这个** |
| Rate Kd | `r_rate_kd` 步长 0.5 | **保持 0** | **保持 0** | 位置式是一阶差分，Roll **可用**；增量式那个是二阶差分，全是噪声 |
| Angle Kp | `r_angle_kp` 步长 0.2 | `p_angle_kp` 步长 0.2 | `y_angle_kp` 步长 0.1 | 消静态倾角，**比直觉小一到两个数量级** |
| Angle Ki | `r_angle_ki` 步长 0.01 | **保持 0** | **保持 0** | **Roll 必须给** —— 占空比里只有"角度的积分"这一项给得出回正力矩，`∝角度`那一项只是阻尼。Pitch/Yaw 的积分器一两拍就顶死，保持 0 |
| Angle Kd | `r_angle_kd` 步长 0.2 | **保持 0** | **保持 0** | 参考工程在 Roll 角度环用到 kd，可试 |
| Ang Lim | `r_angle_limit` 默认 250，步长 5 | 无 | 无 | **角度环输出的硬限幅(°/s)**，范围 0~500，0=不限。**它同时决定静态倾角下的输出上限 = `\|r_rate_kp\| × 它`** —— 给小了 `Angle Kp` 加多大都没用 |
| Speed Kp | `r_rcy_kp` 步长 0.001 | `p_vel_kp` 步长 0.001 | 无 | 位置式外环，输出是**倾角命令(°)**，超过 3° 就是大了 |
| Speed Ki | **保持 0** | **保持 0** | 无 | |
| Speed Kd | **保持 0** | **保持 0** | 无 | `r_rcy_kd` 尤其不能给，霍尔阶梯噪声 |
| Rcy Lim | `r_rcy_limit` 默认 3.0，步长 0.5 | 无 | 无 | **回收环输出的硬限幅(°)**，范围 0~15，0=不限。别靠放大它解决抖动 |
| Rcy Tau | `r_rcy_tau` 默认 2.0，步长 0.2 | 无 | 无 | **回收环反馈的低通时间常数(s)**，范围 0~10，0=不滤波。飞轮差速里的平衡纹波是待纠正漂移的十几倍，不滤掉回收环会跟平衡环共振。**一开回收环就抖，先把它加到 3** |

范围：`*_rate_kp` ±2000/±500/±200，`*_rate_ki` ±200/±50/±50，`*_angle_kp` ±2000/±2000/±50，
`r_rcy_*` ±50/±20，`p_vel_*` ±50/±20（按 Roll/Pitch/Yaw 顺序）。
越界会被钳位，不会写进去一个非法值。

**步长是按每个环的实际落点定的**：从 0 按十下左右就到起手值，
按到落点上限一百下以内，停在最终值附近时一下改动 1%~3%。
起手值和落点区间见 `控制调试指南.md` 第 5 节。

编译默认全 0。Flash 里存过的会盖掉默认值，进页面先看当前值。

**动作行**：

- `Start Rate Test` 只开角速度环
- `Start Angle Test` 开角速度环 + 角度环
- `Start Speed Test` 再开最外环

再按一次同一行停，或者按返回键停。测试期间轴和最高环锁定，串口只能改当前已启用范围内的 PID。

测试开着的时候屏幕下面出实时行：

```text
RATE  +12.30 ANG  -1.20
CMD A -1500 B +1500
RPM A  -820 B   +810
```

停机会在状态行写原因，对照表见 `控制调试指南.md` 第 8 节。

**Rate 和 Angle 不能长时间挂着** —— 这两个模式下飞轮回收环是断开的，飞轮会一路加速到极速触发驱动堵转保护，那个保护是闩锁的，必须重启驱动。

## Camera

进这一页会自动启动摄像头，并自动开 `trk` 波形。

```text
> Exposure          48
  Road Near        133
  Road Far          30
  Error Zero       0.0
  Track Gain      1.00
  Base Speed         0
  Spd Up          1.20
  Spd Down        1.90
  Save
  Back
```

| 行 | 参数名 | 默认 | 步长 | 范围 | 怎么调 |
|---|---|---|---|---|---|
| Exposure | `cam_exposure` | 48 | 2 | 4~1600 | 去 Image 页看 `BIN` 模式，赛道白、边线黑、分界干净就行。太大白区糊成一片，太小整片发黑 |
| Road Near | `road_wide_near` | 133 | 1 | 8~176 | 近端标准赛宽（像素）。单边补线靠它。车摆正在直道中间，量 `BIN+L` 里近端两条边线的像素间距 |
| Road Far | `road_wide_far` | 30 | 1 | 8~176 | 远端标准赛宽。同上，量前瞻最远那行 |
| Error Zero | `err_offset` | 0.0 | 0.5 | ±90 | 中线零点，单位是像素。车摆正在直道中间，`ERR` 不是 0 就往回补这个差值 |
| Track Gain | `track_err_gain` | 1.00 | 0.05 | ±20 | 偏差转航向的增益。大了摆头，小了切不进弯 |
| Base Speed | `track_base_speed` | 0 | 1 | 0~200 | 基准速度目标。Run 没做完之前保持 0 |
| Spd Up | `speed_up_rate` | 1.20 | 0.1 | 0~100 | 加速斜坡速率 |
| Spd Down | `speed_down_rate` | 1.90 | 0.1 | 0~100 | 减速斜坡速率 |

页面下方四行实时数据：

```text
G  120 B 890 E 4200 M 300      各段耗时(us)：取图 / 二值化 / 提边 / 元素
US  5510 MAX  8020             本帧总耗时 / 开机以来峰值
F 30 VS 60 DM 60 DP 30         出帧率 / VSYNC / DMA完成 / 忙丢帧
V1 E   +2.3 L 3 R 0            Track_Valid / 偏差 / 左右丢线行数
```

`US` 超过 16670 就会被 VSYNC 隔帧丢掉，帧率对半掉。`DP` 就是这么丢的帧数。看 `G/B/E/M` 哪段大就优化哪段。

## Element

进这一页也会自动启动摄像头并开 `trk` 波形。十四行参数，超过一屏会滚动。

**默认全关。普通循迹跑稳之后一次只开一个。**

| 行 | 参数名 | 默认 | 步长 | 范围 | 怎么调 |
|---|---|---|---|---|---|
| En Zebra | `elem_en_zebra` | 0 | 2 | 0/1 | 斑马线使能。步长比范围大，上键必开下键必关 |
| En Cross | `elem_en_cross` | 0 | 2 | 0/1 | 十字使能 |
| En Ring | `elem_en_ring` | 0 | 2 | 0/1 | 环岛使能 |
| En Ramp | `elem_en_ramp` | 0 | 2 | 0/1 | 坡道使能 |
| Zebra Jump | `zebra_jump_cnt` | 8 | 1 | 0~60 | 一行里的黑白跳变次数超过它才算斑马线。误判就加大 |
| Cross Lost | `cross_lost_cnt` | 15 | 1 | 0~80 | 双边丢线行数超过它才算十字。误判就加大 |
| Ring Angle | `ring_angle` | 340 | 5 | 0~720 | 环岛累计转角阈值(°)。出环早了减小，晚了加大 |
| Ring CntL | `ring_s2_cnt_l` | 300 | 10 | 0~5000 | 左环状态 2 的编码器累计阈值 |
| Ring CntR | `ring_s2_cnt_r` | 400 | 10 | 0~5000 | 右环状态 2 的编码器累计阈值 |
| Ring Ofs | `ring_side_offset` | 20 | 1 | 0~80 | 环内单边巡线的横向补偿。贴内圈就减小，往外飘就加大 |
| Ring TmO | `ring_timeout_cnt` | 250 | 10 | 0~5000 | 环岛单个状态最长停留帧数，约 5 秒，超了强制回空闲。**卡死时的兜底，别关** |
| Guard Cnt | `elem_guard_cnt` | 40 | 5 | 0~1000 | 元素退出后的屏蔽帧数，约 0.8 秒。同一个元素被连判两次就加大 |
| Ramp Gain | `speed_ramp_gain` | 0.80 | 0.05 | 0~1.5 | 坡道速度倍率 |
| Ring Gain | `speed_ring_gain` | 0.80 | 0.05 | 0~1.5 | 环岛降速倍率 |

页面下方两行：

```text
ELEM RING   RING 2
SCALE 0.80  STOP 0
```

`ELEM` 取值 `NONE` / `ZEBRA` / `CROSS` / `RINGL` / `RINGR` / `RAMP`，`RING` 是环岛状态号（0=空闲），`SCALE` 是建议速度倍率，`STOP` 是停车请求。后两个 Run 还没用。

互斥关系是写死的：环岛中不判十字，十字或坡道中不判环岛，三者任一进行中不判路障。

## Motor

**发车前必须先过这一页。** 方向在这里定死，之后所有极性问题都在 PID 增益的符号上解决，不要回头翻这里。

```text
> Dir A              1
  Dir B              1
  Dir C              1
  Enc C             -1
  Duty Fly        2500
  Duty Drv         800
  Fly RpmMx       7000
  Fly Slew         800
  Jog A Fwd
  Jog A Rev
  Jog B Fwd
  Jog B Rev
  Jog C Fwd
  Jog C Rev
  Save
  Back
```

| 行 | 参数名 | 默认 | 步长 | 范围 | 干什么 |
|---|---|---|---|---|---|
| Dir A | `motor_dir_a` | 1 | 2 | ±1 | A 轮极性。**同时作用于占空比下发和转速回读**，必须同源 |
| Dir B | `motor_dir_b` | 1 | 2 | ±1 | B 轮极性，同上 |
| Dir C | `motor_dir_c` | 1 | 2 | ±1 | C 轮输出极性 |
| Enc C | `enc_dir_c` | -1 | 2 | ±1 | C 轮编码器计数极性 |
| Duty Fly | `jog_duty_fly` | 2500 | 100 | 0~10000 | A/B 点动占空比 |
| Duty Drv | `jog_duty_drive` | 800 | 100 | 0~3500 | C 点动占空比。架空空载，验方向不用转快 |
| Fly RpmMx | `fly_speed_limit` | 7000 | 100 | 0~20000 | 飞轮软超速保护(RPM)，超了自动停并报 `FLYWHEEL OVERSPEED`。0=关掉。**抢在驱动堵转保护之前停，别关** |
| Fly Slew | `fly_slew` | 800 | 100 | 0~10000 | 占空比每 1 ms 允许的变化量，0=不限。防止零转速下的阶跃电流被驱动误判成堵转 |

六个点动行：按一次开始转，**一直转到再按一次、按返回键或者驱动掉线**。

下面三行实时数据：

```text
AIRBORNE JOG A CMD +2500
RPM  A +1200 B     0
ENC  C  +180 TOT   +24500
```

| 查什么 | 合格标准 | 不对怎么改 |
|---|---|---|
| A/B 指令与转速同号 | `CMD +2500` 配 `RPM A +1200` | 翻 `Dir A` / `Dir B` |
| C 正转是车往前 | 车往前走 | 翻 `Dir C` |
| C 编码器与转向同号 | 往前走 `ENC C` 为正 | 翻 `Enc C` |

飞轮低速时 RPM 是阶梯跳变的，霍尔六步一个电周期只有 6 个状态，属正常。

点动不跑姿态闭环，也不查保护角，只能架空，随时准备按返回键。

## Zero

```text
> Roll Zero       0.00
  Pitch Zero      0.00
  Roll Prot       50.0
  Pitch Prot      50.0
  Capture Zero
  Save
  Back

NOW  R   1.23 P  -0.45
ZERO R   0.00 P   0.00
```

| 行 | 参数名 | 默认 | 步长 | 范围 | 干什么 |
|---|---|---|---|---|---|
| Roll Zero | `roll_zero_init` | 0.00 | 0.1 | ±45 | 横滚机械零点(°) |
| Pitch Zero | `pitch_zero_init` | 0.00 | 0.1 | ±45 | 俯仰机械零点(°) |
| Roll Prot | `roll_protect` | 50.0 | 1 | 1~90 | 横滚保护角(°) |
| Pitch Prot | `pitch_protect` | 50.0 | 1 | 1~90 | 俯仰保护角(°) |

`NOW` 是当前姿态角，`ZERO` 是已保存的零点，对着看就知道差多少。

**`Capture Zero`** 把当前姿态角写进零点。用之前：车按平衡姿态摆好、静止、IMU 标定通过、姿态收敛、三个电机全停。任一条不满足会拒绝并在状态行说原因。

保护角判的是**偏离机械零点**的量，不是绝对角。零点没标好会让保护角提前触发。50° 是台架整定用的宽松值，落地前按实车收紧。

## Save：两个 Save 存的范围不一样

| 在哪 | 存什么 | 状态行 |
|---|---|---|
| **子页里的 `Save`**（Roll/Pitch/Yaw/Camera/Element/Motor/Zero） | **只存当前这一页的参数** | `Roll PAGE SAVED` |
| **`Params` 最底下的 `Save`** | **存全部参数** | `ALL SAVED TO FLASH` |

子页的 Save 不会碰别的页：别的页在 Flash 里是什么值，存完还是什么值。
所以可以在 Roll 页调完存 Roll，再去 Pitch 页调完存 Pitch，两边互不影响。

反过来也要注意：**在 Roll 页改了 Camera 的参数，按 Roll 页的 Save 是存不进去的**。
串口改的参数同理，只有对应页的 Save 或 Params 页的 Save 才写得进去。

从来没存过、又不在当前页里的参数，子页 Save 也不会替它写记录，下次上电照样回默认值。

两个 Save 都写 Flash 扇区 0 第 11 页，写完回读逐字比对，状态行报成功或失败。
电机在跑（Test、点动、Balance）时一律拒绝，状态行出 `SAVE BLOCKED: RUNNING`。

**不 Save 掉电就没了。**

参数按名字哈希做键，加参数、删参数、调顺序都不影响其他参数已存的值，所以不需要版本号。只有改参数名或者把 int 改成 float 才等于换了键，那一项会回默认值。

## Reset

擦除参数页并回默认值。**要按两次确认**，第一次按会进确认态，3 秒内不按第二次就自动撤销。光标一移开也撤销。

零点、方向、所有整定好的 PID 全部丢失，慎用。
