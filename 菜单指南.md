# 菜单指南

核对日期：2026-09-07，以 [menu.c](code/menu.c) 的页面和动作表为准。各参数默认值、类型、范围及步长集中在 [参数参考](参数参考.md)，避免旧菜单截图与当前参数混用。

## 页面与按键

```text
MENU
├─ Params
│  ├─ Attitude
│  ├─ Roll / Pitch / Yaw
│  ├─ Lean
│  ├─ Run
│  ├─ Camera → Calib IPM
│  ├─ Element
│  ├─ Motor
│  ├─ Zero
│  ├─ Odometry
│  ├─ Save
│  ├─ Reset
│  └─ Back
├─ Image
├─ Run
└─ Run Test
   ├─ Balance
   ├─ Remote
   └─ Back
```

上/下移动光标，参数行按确认进入编辑，再按确认结束编辑。长按上/下约每 120 ms 连发，页面记住光标和滚动位置；屏幕最多显示 13 行。

返回键在运行基础状态非 STOP 时先调用公共停机；在参数组还会退出编辑或页面。切页会停止本地单轴 Test、Jog 和 1m Test，清普通 VOFA 模式；独立 Balance、Remote 和正式 Run 不因普通翻页自动停止。新版固件与上位机握手后，仍处于 Balance 基础状态或单轴 Test 时会自动发送 run 诊断波形；普通 VOFA 模式关闭不等于车辆停止。

## 主菜单 Run 与 Run Test

| 入口 | 操作 | 结果 |
|---|---|---|
| 主菜单 Run | 3 秒内两次确认 | 通过视觉/IPM和三轴安全条件后正式循迹 |
| 正式 Run 运行中按 Run | 一次确认 | 结束行驶，继续 Balance |
| Run 结束后仍 Balance 时按 Run | 一次确认 | 完全停机 |
| Run Test → Balance | 3 秒内两次确认 | 零速、锁存当前航向的三轴平衡 |
| Balance ON 时同一行确认 | 一次 | 完全停机 |
| Run Test → Remote | 3 秒内两次确认 | 等待无线 speed 命令 |
| Remote ON 时同一行确认 | 一次 | 完全停机 |

Remote 的 `speed:turn,speed` 输入均允许 -90～90；速度输入除以 **40** 转为 m/s，超出 ±1.50 m/s 被拒绝，因此当前有效速度原始值实际为 ±60；目标航向为接收时航向加 steer_dir×turn。例如 speed:0,20 请求 0.50 m/s。超过 1000 ms 未收到有效命令时目标速度和速度斜坡归零，清除旧转向并锁存当前航向，保持平衡。无线命令不能自行启动 Remote。

Run 的失帧、连续丢线及正常终点均可能只结束行驶，详细条件见 [控制调试指南](控制调试指南.md)。车身返回键或串口 stop 是完全停机路径。

## Attitude

显示 Roll、Pitch、连续 Yaw 和姿态状态；确认 Test 开关控制约 50 Hz 的三通道 att 波形。它不启动电机。离页关闭普通姿态波形；cfg hello 后的辅助约 10 Hz att 与页面开关独立。

Yaw 是六轴惯导的连续角，会漂移，不代表有绝对航向参考。标定未通过或姿态尚未收敛时，先解决状态问题再做闭环测试。

## Roll / Pitch / Yaw

Roll、Pitch 各 **9** 行 PID，Yaw **6** 行；参数后是动作、Save、Back。

| 显示行 | Roll 参数 | Pitch 参数 | Yaw 参数 |
|---|---|---|---|
| Rate Kp/Ki/Kd | r_rate_kp/ki/kd | p_rate_kp/ki/kd | y_rate_kp/ki/kd |
| Angle Kp/Ki/Kd | r_angle_kp/ki/kd | p_angle_kp/ki/kd | y_angle_kp/ki/kd |
| Speed Kp/Ki/Kd | r_rcy_kp/ki/kd | p_vel_kp/ki/kd | 无 |

动作显示为 Rate、Angle、Speed；Yaw 没有 Speed。Rate 只启用内环，Angle 加角度环，Speed 再加 Roll 回收或 Pitch 速度环。再次确认同一动作停止；切换最高环前先停止已有 Test。

Roll 回收三项菜单步长为 0.0001，Pitch 速度环为 0.001；原 Ang Lim / Rcy Lim / Rcy Tau 行已删除。不要按旧“12 行 Roll”截图定位参数。当前菜单 Roll Test 的编辑边界还有一个遗留偏差：Angle Test 会额外允许 Speed Kp（第 6 索引）进入编辑，虽然回收环未启用；cfg 权限位图不放行这一项。详见 [源码核对记录](tools/unicycle_station/docs/source-audit.md)。

运行中 Roll/Yaw 页显示角速度/角度、A/B 指令和 RPM；Pitch 页显示角速度、角度及 C 输出。没有自动 roll/pit/yaw 波形。

## Lean 与 Run 参数页

Lean 四行：Turn Kp（`lean_turn_kp`，0～0.1 s，默认 0.01）、Speed Kp（`lean_speed_kp`，0～2，默认 1）、Slew dps（`lean_slew_dps`，1～60°/s，默认 24）、Lean Max（`lean_max_angle`，0～10°，默认 5°），调整正式 Run 的压弯。前三项不继承旧 Lean Kp，详见 [压弯控制说明](压弯控制说明.md)。

Params → Run 是 **12 行参数页**，不是发车按钮：

| 行 | 参数 / 单位 | 默认 |
|---|---|---:|
| Straight / Curve / Cross / Ring / Ramp | run_speed_*，m/s | 均 1.20 |
| Lost | run_speed_lost，m/s | 0.20 |
| Accel / Decel | run_accel_mps2 / run_decel_mps2，m/s² | 0.50 / 1.00 |
| Pixel Kp | direction_pixel_kp | 1.20 |
| Head Kp | direction_heading_kp | 0.92 |
| Curve FF | direction_curve_kff | 50 |
| Dir Kd | direction_rate_kd | 0 |

速度参数范围 0～1.50 m/s；分段实际目标还受 Straight 上限及控制逻辑约束。Zebra Stop 属于 Element 页。

## Camera 与 Image / IPM

Camera 只有 Exposure（默认 48，步长 2）和 Front Row（默认 67，步长 1），然后是 Calib IPM、Save、Back。旧 Road Near / Road Far / Error Zero / Track Gain / Base Speed / Spd Up / Spd Down 已不在参数表中。

Image 显示整帧 188×120 图像；确认切 BIN+L / BIN / GRAY，上下调整曝光，返回退出。摄像头在 CPU1 初始化；页面入口查询就绪状态，失败时请求 CPU1 重启，不表示只有进入页面才采图。

Calib IPM 页显示梯形/角点、READY 或不满足的条件；确认发起多帧标定，无有效 IPM 且持续 READY 约 2 秒也会自动请求。成功矩阵由 CPU0 在停车时自动保存；Camera Save 不保存矩阵。详细步骤见 [视觉调试指南](视觉调试指南.md)。

当前 Camera/Element 普通参数页没有旧版的视觉耗时/丢线多行诊断面板，也不输出 trk；需要看图像和任务状态时使用 Image 或上位机任务页。

## Element

当前 **8 行**，默认只有十字开启。

| 行 | 参数 | 默认 |
|---|---|---:|
| En Zebra / En Cross / En Ring / En Ramp | elem_en_zebra/cross/ring/ramp | 0 / 1 / 0 / 0 |
| Zebra Stop | zebra_stop_offset_m | 0.15 m |
| Ring Angle | ring_angle | 340° |
| Ring CntL / Ring CntR | ring_s2_cnt_l / ring_s2_cnt_r | 300 / 400 counts |

旧 Zebra Jump、Cross Lost、Ring Ofs、Ring TmO、Guard Cnt、Ramp Gain、Ring Gain 不再是可调菜单参数。相关检测/时间阈值集中在 board_config.h，元素发布绝对限速而不是倍率。没有路障功能。

## Motor

9 行参数：Dir A、Dir B、Dir C、Enc C、Steer Dir、Duty Fly、Duty Drv、Fly Max、Fly Slew。默认方向为 +1/+1/+1/+1/-1，点动值 2500/800，飞轮上限 7000 RPM，Fly Slew 为 **0**（不启用斜坡）。

随后六个 Jog 动作分别为 A/B/C 的 Fwd/Rev。一次确认开始持续点动，点动中确认任一 Jog 动作都会停止，离页也停止；没有固定时长。Jog 不跑姿态闭环，不以姿态保护角限制动作，只适合架空验证；A/B 运行时检查驱动失联/超速；C 点动不经过这两个 A/B 门槛。启动 Jog 仍要求通过 IMU/标定/姿态状态检查。

A/B 方向同时作用于占空比及回读，检查实际旋转和车体反作用方向，不能仅凭屏幕 CMD/RPM 同号判定安装正确。C 轮向前时编码器应为正。

Jog 时如果进入参数编辑，第一次返回只退出编辑，尚不停止 Jog；再次返回离开参数页才停止。原因是 Jog 保持 START_STOP 且不设置 s_test_on，绕过两个返回停机判断。需要完全停车时使用退出页面、任一 Jog 动作或串口 stop，不能把一次返回视为所有界面状态下的保证。

## Zero

仅有 Roll Zero、Pitch Zero、Roll Prot、Pitch Prot，随后 Save、Back；**没有 Capture Zero 动作**。显示 NOW 当前姿态和 ZERO 当前运行零点，手动编辑零点再保存。ZERO 不保证已经写入 Flash。

默认零点约 +0.99669° / -3.05225°，保护角 20° / 50°；角度保护相对机械零点判断。范围和步长见参数参考。

## Odometry

Counts 为 `odom_counts_per_m`，默认 11690，步长 10；Test 为 `odom_test_speed`，默认 0.05 m/s，步长 0.01，范围 0.05～0.50。

- Reset Counter：停车且无其他测试时清 C 总计数；手推准确 1m，按实际距离求每米 counts，编辑 Counts 后保存。
- Start 1m Test：**一次确认即启动**三轴平衡直行，不使用 Run/Balance 的两次确认流程。最后 0.20m 按剩余距离降速，最低 0.05 m/s。
- 达到 1m 显示 1M DONE / BALANCING：目标速度归零，仍维持平衡。Stop 1m Test、返回或切页会停止。

页面显示 COUNT、DIST 和 SPEED；里程判断取相对起点距离的绝对值，方向和比例必须先独立校验。

## 编辑与持久化

编辑立即改变 RAM，返回退出编辑不会撤销已改数值；不保存掉电丢失。菜单限制与 cfg 限制分别实现：菜单主要限制运行中 Motor/Zero/Odometry，以及单轴 Test 的已启用环；cfg 在 Balance 基础状态额外开放 Run/Lean 共 14 项速度、转向与压弯参数，其他非 PID 参数仍需停车；单轴 Test 只允许当前参与环的 PID，Jog 全部锁定。不要假设菜单中的可编辑意味着串口也允许，或把现有菜单限制当作所有模式下的完整保护。

| 保存入口 | 范围 | 当前门槛 |
|---|---|---|
| 各参数子页 Save | 本页参数名列表，与已有 Flash 合并 | STOP、无 Test/Jog |
| Params → Save | 全部 74 项 | STOP、无 Test/Jog |
| IPM 自动保存 | 9 个矩阵系数 | 待保存且 STOP、无 Test/Jog |
| cfg:save | all 或指定协议组 | 模式完全停止、稳定轮速、RX 静默、无待存 IPM，详见协议 |

子页 Save 不顺带保存其他页 RAM 中的修改；Camera 页不含 IPM。Flash 按参数名与类型生成键，类型/名称变更会影响旧记录匹配；有 CRC、范围检查及写后逐字回读。不能以菜单离页或 PC 显示新值代替持久化成功。

Reset 要求停车并在 3 秒内两次确认，擦除参数记录并加载当前源码默认值。它会丢失已有标定/整定记录，恢复的 PID 和 IPM **不是全零**。本次文档整理未执行任何车端 Reset 或参数写入。