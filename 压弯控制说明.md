# 压弯控制与验证（2026-09-11）

当前实现以 [control.c](code/control.c) 的 `lean_offset_update()` 为准。此次替换压弯前馈，保留 Pitch/Roll/Yaw PID、飞轮回收、机械零点、Yaw 动量降权和 Roll 优先混控。菜单、cfg、上位机源码 Profile 与示例工作区同步；运行参数由 72 项变为 74 项。现有 EXE、用户另存的工作区和车辆固件不会随源码自动更新。

## 原来的压弯为何可能不明显

旧算法用 `(目标航向−当前航向) × C轮20ms计数 × lean_roll_kp / 20000`，并要求原始加权视觉偏差绝对值大于 1000。进入稳定转弯后，航向跟踪误差可以变小，中线居中时视觉偏差也可以很小；两者都不意味着车辆已经不转弯，却会让旧压弯变小或关掉。旧版还固定限制为每 5 ms 变化 0.08°，从 0 到 5°至少需要 0.3125 s。

这是源码确认的目标生成问题，不能据此断定每次饱和倒车的原因。增大 Lean Max 只改变天花板，不能让门控关闭或接近零的原始目标变大。

## 对两张参考图的取舍

采用“方向预压弯＋随速度增强的压弯”，仍把它作为 Roll 角度目标的偏移加入。第一张图中的独立正向速度项 `K2*v` 若没有额外方向条件，会在直行时产生固定侧倾，因此改为车速与转弯率相乘。

理想稳态转弯满足 `tan(phi)=v²/(gR)`。当转弯率 `r=v/R` 时得到 `tan(phi)=v*r/g`；这是本次速度项的依据，不能直接证明动量轮独轮车的瞬态性能。[伊利诺伊大学力学说明](https://van.physics.illinois.edu/ask/listing/14948)

第二张图的 `v²*alpha` 在 alpha 表示实际曲率 `1/R` 时有相同物理含义。本项目视觉曲率经过归一化，不能直接当作 1/m；使用已经形成、经过动量降权和斜率限制的转弯指令，单位是 °/s。该指令包含像素、航向及曲率前馈。没有采用逐拍 `Machine_zero += K1*alpha`：它会积累偏置，并把增益与调用周期绑定；当前机械零点始终独立保留。

## 当前公式和状态处理

每 5 ms 执行，定义：

```text
v_f += 0.10 * (clip(实际车速, -1.5, 1.5) - v_f)       # m/s
r = clip(斜率限制后的转弯指令, -120, 120)              # °/s，run CH9
r_db = sign(r) * max(abs(r) - 2, 0)                  # 连续死区
fade = clip((abs(v_f) - 0.05) / 0.25, 0, 1)
lean_raw = sign(v_f) * fade * r_db * (Kturn + Kspeed*abs(v_f)/9.80665)
lean_target = clip(lean_raw, -LeanMax, LeanMax)
lean_offset = 向 lean_target 每拍最多移动 SlewDps * 0.005 度
Roll目标 = 机械零点 + clip(LEAN_DIR*lean_offset - 回收输出, -10°, 10°)
```

这里 `v*r/g` 用 °/s 输入时直接输出小角度近似的度数；10°附近近似误差约 1%。Kturn 单位为秒，Kspeed 是倍率。车速滤波时间常数约 45 ms；低于 0.05 m/s 不主动压弯，0.30 m/s 起使用全权重。转弯率死区采用减去 2°/s 的连续形式，不是在阈值处突然跳入完整角度。

启用条件是正式 Run、本次 Run 视觉已握手、循迹和 IPM 有效、视觉年龄 <100 ms、方向非 HOLD，且输入/增益为有限值。其他情况下压弯函数把目标置零并清空车速滤波状态，输出按斜率回零。公共 STOP/串级复位、Roll 未启用，以及 `run_hold_balance()` 等原有退出路径直接清零压弯和滤波状态；本次未改变这些退出动作。独立 Balance、Remote 和单轴 Test 不主动产生这项视觉压弯。

速度项和方向项都随低速权重淡出；直行不会仅因车速为正而侧倾。对同一转弯率，反向车速反转侧倾符号。保留现有 `LEAN_DIR=1`，它与实际车身 Roll/Yaw 安装符号是否对应弯内倾斜，必须在实车核对，不能由正负数字推断。不要为纠正压弯方向而随意反转整个 STEER_DIR。

## 新参数与旧配置

| 菜单 / 协议名 | 默认值 | 范围 | 步长 | 作用 |
|---|---:|---:|---:|---|
| Turn Kp / `lean_turn_kp` | 0.010 s | 0～0.100 s | 0.005 | 方向预压弯；转弯指令变化时增加侧倾目标 |
| Speed Kp / `lean_speed_kp` | 1.00 | 0～2.00 | 0.05 | `v*r/g` 项倍率 |
| Slew dps / `lean_slew_dps` | 24°/s | 1～60°/s | 1 | 入弯、反向和正常回正的压弯变化速度 |
| Lean Max / `lean_max_angle` | 5° | 0～10° | 0.1 | 压弯目标幅值上限 |

默认斜率从 0 到 5°至少需要约 0.208 s；方向指令斜率、车速滤波和实际 Roll 跟踪还会增加响应时间。降低 Lean Max 时已有输出按斜率逐步退回新上限内；软件绝对硬上限和压弯/回收合成目标仍是 ±10°。

Flash 按名称与类型的哈希键保存。旧 `lean_roll_kp` 已退休，不转换为新增益，也不再接受 cfg 写入；新三项在首次读取旧 Flash 时保持上述默认值。已有有效的 `lean_max_angle`、PID、零点、IPM 等仍按原键加载，所以实际 Lean Max 可能是之前保存的 8°或 10°，不一定是 5°。无需 Reset 全部参数。用新版本保存后，旧增益记录不再保留；旧固件回退时该旧增益将回到旧默认值。历史日志中的旧名称保留其原含义。

Balance/Run/Remote 中 cfg 对这些参数开放已有的在线控制白名单权限（Schema bit4＋stat bit24）；单轴 Test/Jog 不因此开放。可写不代表此模式压弯已启用。修改只写 RAM，停车后按原 Save 流程保存。

稳定输入时的公式示例，Kturn=0.01、Kspeed=1，已计入 2°/s 死区：

| 实测速度 | 转弯指令 | 未限幅目标 | 默认 Lean Max=5° 后 |
|---:|---:|---:|---:|
| 0 m/s | 60°/s | 0° | 0° |
| 0.6 m/s | 30°/s | 约 1.99° | 约 1.99° |
| 1.0 m/s | 60°/s | 约 6.49° | 5° |
| 1.2 m/s | 60°/s | 约 7.68° | 5° |

这些是目标计算值，未仿真车辆运动。若 CH9 很大而实际 Yaw rate 很小，指令对应的侧向加速度并未实现；继续加压弯可能造成过度倾斜，应先检查转向权限和跟踪能力。

## 实车验证和调节顺序

先读取并留存车端参数，确认新 Schema 包含三项新名称。核对 C 轮里程标定和实测车速：该算法使用 m/s，`odom_counts_per_m` 错误会直接影响压弯大小。先保持已调好的 PID 和机械零点；原地左右摇晃应单独按 Roll 诊断处理，新压弯在独立 Balance 不启用。

1. 选择已有可控的低速、同一路段。先将两个 Kp 都设为 0 做无压弯基线，再按默认新增益开启；首轮可把 Lean Max 暂设 2～3°核对左右弯方向。保持同样速度、路线、PID 和起始飞轮转速，一次只改一个量。用视频观察车身是否朝弯内侧倾。
2. **CH7 不起量**：检查 Run/视觉/HOLD 状态、CH9 转弯指令和 CH20 实测速度，再检查 Kp 与 Lean Max。CH7 已顶到 Lean Max 时，增大 Kp不会增加稳态输出。
3. **CH7 已变化但净目标没变化**：同时看 CH5 回收和 CH2 Roll 目标。用 `CH2−roll_zero` 判断合成后实际请求；回收抵消压弯或合成目标到 ±10°时，继续加前馈未必有收益。不要据此直接关闭回收。
4. **CH2 先变，CH1 跟不上**：问题在 Roll 跟踪、执行器余量或姿态估计。先比较跟踪延迟和 Roll 输出，避免仅靠更大的侧倾目标补偿。若 CH7 本身入弯明显晚、CH1 能跟随，可把 Slew dps 从 24 小步加到 30，再看是否引入左右摆动；若已稳定转弯但目标不足，优先逐步调 Speed Kp，Turn Kp 用 0.005 的步长补充方向响应。
5. **是否改善转向**：同速对照 `CH9−CH10` 的 Yaw rate 跟踪误差、CH11/CH12 请求与实际分配的裁剪、CH13 共模转速及其增长、CH21 动量权限、轨迹偏差和连续可通过弯道数量。Lean Max、Kp越大不自动代表越好，若摆动/裁剪/单轮转速恶化就恢复上一步。

run 帧编号从 CH0 时间开始，含义见 [通道说明](tools/unicycle_station/docs/channels.md)。CH7 是未乘 LEAN_DIR、未扣回收的指令；CH1 是融合估计角，不是独立的真实侧倾测量。加速度计测倾角以重力为主要参考，持续侧向加速度可能造成偏差，不能只凭上位机姿态图认定压弯实现；使用固定视角视频或独立姿态参考对照。[Analog Devices AN-1057](https://www.analog.com/en/resources/app-notes/an-1057.html)

飞轮逐个转速上限仍需观察。同步数据下，令 `M=(A+B)/2`、`D=A−B`，单轮最大绝对值为 `|M|+|D|/2`；CH13 共模较小不代表每个轮都有余量。当前压弯是前馈，既不增加电机最大力矩，也不是飞轮主动卸载。若上述对照显示共模持续增长，应继续评估转弯速度、轮地力矩和动量管理。

## 软件验证记录

2026-09-11：`python tools/ads/build.py` 通过 ADS/TASKING Debug 构建，0 errors，48.8 s；仅报告现有 `vofa_task.inc:35` 的 W515 警告。新 ELF/HEX/MAP 及源码哈希验证通过，路径和 SHA-256 见 [.ads/build/latest.json](.ads/build/latest.json)。该文件描述最近一次构建，后续构建会更新它。本次输入哈希为 `0b6dc3572084d55567efbc480be639736ba8fb8631c943fd2b9ff8759389504b`。

25 项针对性主机回归通过：从真实 C 源码提取并运行压弯和 Flash 初始化路径，覆盖持续转弯、左右/倒车对称、低速/死区、双项关闭、角度限幅/斜率、视觉与非法值退出、旧键忽略与其他参数保留、CRC 拒绝，并验证 cfg 协议和上位机连接、在线参数与遥控回归。主机测试不测 ISR 最坏耗时、车辆动力学或实际闪存硬件。本次未烧录、未写车端实时参数、未进行新算法实车验证。
另已在独立临时目录验证 Profile 生成器：74 项，四项 Lean 的名称、范围、默认值、步长与源码一致；本地开发 skill 校验通过。Mock 离屏检查完成参数应用与回读，工作台显示 Lean 四项和相关波形；这些模拟曲线不代表新控制算法的实车响应。
