# 控制调试指南

## 1. Motor 点动，先把方向定死

`Params → Motor`，六个动作行依次点，再按一次同一行停。屏幕下面三行同时看：

```text
AIRBORNE JOG A CMD +2500
RPM  A +1200 B     0
ENC  C  +180 TOT   +24500
```

| 查什么 | 合格标准 | 不对怎么改 |
|---|---|---|
| A/B 指令与转速同号 | `CMD +2500` 配 `RPM A +1200` | 翻 `Dir A` / `Dir B` |
| C 正转是车往前 | 车往前走 | 翻 `Dir C` |
| C 编码器与转向同号 | 往前走 `ENC C` 为正 | 翻 `Enc C` |

方向没定死之前不要动 PID。之后再出现极性问题，一律改 PID 增益的符号，不要回来翻 `Dir`——
`Dir A/B` 同时作用于占空比下发和转速回读，翻了它回收环会变成正反馈。

## 2. Test 规则

| 动作行 | 开了哪几个环 |
|---|---|
| Rate | 只开角速度环 |
| Angle | 角速度环 + 角度环 |
| Speed | 再开最外环，Roll 是回收环，Pitch 是速度环 |

Yaw 没有 Speed。

启动时清 PID、清输出、目标航向对齐当前 Yaw。测试期间轴和最高环锁定，串口只能改当前已启用范围内的 PID。按返回键随时停。

## 3. 上电时 24 个增益全是 0

`board_config.h` 里所有 `*_DEFAULT` 都是 `0.0f`，Flash 没存过值就全是 0。
**这 24 个里只有 10 个要调，剩下 14 个整个调试过程都保持 0。**

### 哪些永远是 0

**除了 `r_angle_ki`，其余位置式环的 ki 和 kd 都保持 0。**

> `r_angle_ki` 是例外，而且是必须给的一项 —— 它是整条 Roll 链里唯一的回正来源，
> 理由见第 7 节。它的 `imax` 专门放到了 2000，其余环仍是 50。

位置式的积分是 `integrator += error` 后钳到 `±imax`，没有乘 dt。其余环的 `imax` 是 50（`y_rate` 是 100），
而这些环的误差量级是：角度环几度、回收环几百 RPM、速度环几十到几百 counts。
误差稍大一点，积分器一两拍就顶到限幅不动了，`out_i` 变成 `±ki×imax` 这么个只跟符号有关的常数偏置，
不是积分，是个会来回跳的死区补偿。给了只会抖，所以一律 0。

**所有 kd 保持 0。**内环 kd 是二阶差分，全是噪声；`r_rcy_kd` 更特殊——霍尔六步在低转速下回传的转速是阶梯值，
对台阶做微分等于每个台阶发一根尖刺。

### 哪些要调

只有 10 个：

| 轴 | 环 | 要调的 |
|---|---|---|
| Roll | `r_rate` | `kp` `ki` |
| Roll | `r_angle` | `kp` **`ki`** |
| Roll | `r_rcy` | `kp` |
| Pitch | `p_rate` | `kp` `ki` |
| Pitch | `p_angle` | `kp` |
| Pitch | `p_vel` | `kp` |
| Yaw | `y_rate` | `kp` |
| Yaw | `y_angle` | `kp` |

## 4. 只有 Pitch 内环是增量式

| 环 | 实现 | 主增益 | ki |
|---|---|---|---|
| `p_rate`（C 行进轮） | `pid_inc_calc_limited()` 增量式 | `ki` | **要调** |
| `r_rate`（A/B 动量轮） | `pid_loc_calc()` **位置式** | `kp` | **保持 0** |
| `y_rate`（A/B 动量轮） | `pid_loc_calc()` **位置式** | `kp` | **保持 0** |

**Roll 内环刻意不用增量式。**增量式的 `out` 是个永久累加器：误差回到 0 时输出停在原地不动，
占空比恒定 → 飞轮到极速 → `dω/dt=0` → 反作用力矩为零 → 车必倒。
实测就是"能站 2~3 秒然后直接倒"，而且倒的瞬间飞轮都在 96%~99% 极速上。
C 行进轮没有这个失效模式（蹬地力矩不靠加速度），所以 Pitch 保留增量式。

迁移时增益不用重调：增量式在 `ki=0` 时逐拍累加会望远镜化成 `out = kp·e`，
和位置式的 P 路完全等价，`r_rate_kp` 原样搬过去就行。

### 增量式内环（`p_rate`）：先给 ki，ki 才是扶车的那一路

增量式每拍往输出上加 `kp·Δe + ki·e`，累加之后等效于：

```text
out ≈ kp·e + ki·Σe
```

内环误差 `e` 就是角速度（°/s），1ms 累加一次，所以 `Σe = 1000 × 角度(°)`：

- `kp` 那一路 = 增益 × 角速度 → **阻尼**
- `ki` 那一路 = 增益 × 1000 × 角度 → **回正**

所以 `ki` 给 0 的话电机基本不动，车只会慢慢倒。顺序一律是 **ki → kp**。

这也给了一个能直接算的起手值：

```text
ki ≈ 想要的占空比 ÷ (1000 × 对应的倾角°)
kp ≈ 想要的占空比 ÷ 对应的角速度(°/s)
```

例：想让 5° 倾角出 2000 占空比 → `ki = 2000/(1000×5) = 0.4`；
想让 100°/s 出 2000 占空比 → `kp = 2000/100 = 20`。

### 位置式内环（`y_rate`）：只给 kp

误差是航向角速度（°/s），输出直接就是占空比：

```text
kp ≈ 想要的占空比 ÷ 角速度误差(°/s)
```

`y_rate_ki` 保持 0（理由见第 3 节）。

**`r_rate` 也是这个算法**，`kp` 是主增益、`ki` 保持 0。
区别是它的 `kd` 可用：位置式的 kd 是一阶差分（角加速度），不是增量式那个全是噪声的二阶差分。
两个参考独轮工程的飞轮内环都重用 kd，量级约 `0.5 × |kp|`，符号跟 `kp` 一致。

### 外环 kp 都是单位换算，能反推

每个外环的输出是下一环的**输入量纲**，把它当换算系数看就能直接估：

| 环 | 输入 | 输出 | 起手值怎么定 |
|---|---|---|---|
| `r_angle` | 横滚误差(°) | 角速度命令(°/s) | 允许的最大角速度命令 ÷ 典型误差角 |
| `p_angle` | 俯仰误差(°) | 角速度命令(°/s) | 同上 |
| `y_angle` | 航向误差(°) | 角速度命令(°/s) | 同上 |
| `r_rcy` | 飞轮转速差(RPM) | 倾角命令(°) | 允许的最大倾角 ÷ 典型转速差 |
| `p_vel` | counts/20ms | 倾角命令(°) | 允许的最大倾角 ÷ 典型计数 |

`r_rcy` 和 `p_vel` 的输出是加进角度环误差里的，等于在挪机械零点，所以"允许的最大倾角"给 2~3° 就够，
给大了车会明显歪着走。

## 5. 起手值总表

下面的数是**起手值和预期落点**，不是标定值。每次乘 2 往上加。

> Roll 三行的量级参考了两个实车独轮工程（`独轮`/TC297 与 `smart_car`/TC264），
> 它们的飞轮串级都是位置式、积分项全为 0、每个环都有独立输出限幅。符号必须实测确定。

"别超过"那一列的意思是：**加到这个值还是没现象，问题不在这个增益上**，回去查方向、查零点、查波形符号，
继续加只会在某一次直接炸。

| 参数 | 起手 | 常见落点 | 别超过 | 菜单步长 | 备注 |
|---|---|---|---|---|---|
| `r_rate_kp` | −40 | −20 ~ −60 | ±200 | 1.0 | **位置式主增益**，符号由实测定 |
| `r_rate_ki` | — | — | — | 0.01 | **保持 0**，imax=100 一两拍就顶死 |
| `r_rate_kd` | 0 | 0.5×\|kp\| | ±200 | 0.5 | 位置式的一阶差分，**现在可用**，符号跟 kp 一致 |
| `r_angle_kp` | 15 | 15 ~ 60 | 200 | 0.2 | 回正力全靠它，内环没有积分路 |
| `r_angle_ki` | 0.05 | 0.05 ~ 0.5 | 5 | 0.01 | **Roll 唯一的回正来源，必须给**，符号跟 kp 一致 |
| `r_angle_kd` | 0 | 0 ~ 4 | 50 | 0.2 | 参考工程用到 2.5×kp，可试 |
| `r_angle_limit` | 250 | 200 ~ 400 | 500 | 5 | 硬限幅(°/s)。**静态输出上限 = \|r_rate_kp\| × 它**，给小了 angle_kp 加多大都没用 |
| `r_rcy_kp` | 0.002 | 0.001 ~ 0.01 | 0.1 | 0.001 | 低通开着时的落点 |
| `r_rcy_ki` | — | — | — | 0.001 | **保持 0** |
| `r_rcy_kd` | — | — | — | 0.001 | **保持 0**，霍尔阶梯噪声 |
| `r_rcy_limit` | 3.0 | 1 ~ 3 | 15 | 0.5 | 不是增益，是回收环输出的硬限幅(°) |
| `r_rcy_tau` | 2.0 | 1 ~ 3 | 10 | 0.2 | 不是增益，是回收环反馈的低通时间常数(s)。**一开回收环就抖，先加大它** |
| `p_rate_ki` | 0.05 | 0.1 ~ 0.8 | 2 | 0.01 | 先调它 |
| `p_rate_kp` | 2 | 5 ~ 50 | 150 | 0.5 | |
| `p_rate_kd` | — | — | — | 0.5 | **保持 0** |
| `p_angle_kp` | 1 | 2 ~ 20 | 50 | 0.2 | |
| `p_angle_ki` | — | — | — | 0.01 | **保持 0** |
| `p_angle_kd` | — | — | — | 0.2 | **保持 0** |
| `p_vel_kp` | 0.005 | 0.005 ~ 0.05 | 0.2 | 0.001 | |
| `p_vel_ki` | — | — | — | 0.001 | **保持 0** |
| `p_vel_kd` | — | — | — | 0.001 | **保持 0** |
| `y_rate_kp` | 2 | 5 ~ 50 | 100 | 0.5 | 位置式，只调 kp |
| `y_rate_ki` | — | — | — | 0.01 | **保持 0** |
| `y_rate_kd` | — | — | — | 0.5 | **保持 0** |
| `y_angle_kp` | 0.5 | 1 ~ 5 | 20 | 0.1 | |
| `y_angle_ki` | — | — | — | 0.01 | **保持 0** |
| `y_angle_kd` | — | — | — | 0.1 | **保持 0** |

步长是按落点定的：**从 0 按十下左右就到起手值，按到落点上限一百下以内**，
停在最终值附近时一下改动 1%~3%。所以键调和串口调都行：

- 键调：长按连发 120ms 一下，按到起手值一两秒。
- 串口：一次写死一个值，翻倍的时候更快，`r_rate_ki 0.4` 比按 30 下省事。

命令清单见 `调参命令.md`。

## 6. A/B 和 C 的出力方式完全不同，别拿一个的现象去套另一个

这是最容易看岔的一条：

| | C 行进轮（Pitch） | A/B 动量轮（Roll / Yaw） |
|---|---|---|
| 力矩怎么来 | 轮子蹬地，**地面给的反作用力** | 飞轮加减速，**角动量交换** |
| 力矩正比于 | 轮子的驱动力（≈ 占空比） | 飞轮的角**加速度** `dω/dt` |
| 占空比恒定时 | 轮子持续往前跑，支撑点持续追重心，**恢复力矩一直在** | 飞轮稳到一个转速，`dω/dt→0`，**力矩归零** |
| 能不能一直顶住静态倾角 | 能，只要地方够跑 | 不能，飞轮必然饱和 |

所以 **"C 轮的角速度环没问题，歪了能自己恢复"是正常的，
"A/B 只有快速推它才有反应、静态歪着就匀速转"也是正常的** —— 这两件事不矛盾，
是两种执行器的物理差别，不是哪个轴调坏了。

也因此，Pitch 那一轴的手感不能拿来当 Roll 的验收标准。

## 7. 飞轮给的是"加速度力矩"，不是"转速力矩"

**动量轮给车体的反作用力矩正比于飞轮的角加速度 `dω/dt`，跟飞轮转多快没有关系。**
飞轮匀速转 = 零力矩。要一直有恢复力矩，就得一直让飞轮加速。

而主控下发的是占空比，不是力矩。占空比给定后飞轮会稳到一个转速就不再加速，
**力矩自己就归零了**。所以要维持力矩，必须让占空比持续往上爬。

由此有三条直接的后果：

**一、Rate 模式下静态偏角没有反应，是正常的，不是坏了。**

Rate 模式的误差就是角速度。车静止歪在那儿，角速度是 0，`ki·e` 每拍加的是 0，
输出冻在上一个值不动 → 飞轮匀速 → 力矩为零 → 车就那么歪着不动。

**推得快才顶得住、慢慢压过去它就不管**，说的就是这件事。Rate 模式只能判动态，不能判静态。

**二、要让静态偏角也有恢复力矩，必须开 Angle 环。**

角度环让内环误差在角速度为 0 时也不为 0：

```text
内环误差 = roll_rate + r_angle_kp·(roll - zero)
```

车静止歪着，右边那项不为 0，`ki·e` 就每拍继续往输出上加 → 占空比持续爬 →
飞轮持续加速 → 持续力矩。**所以"歪着能自己回正"是 Angle 环的验收项，不是 Rate 环的。**

**三、只有"角度的积分"才给得出回正力矩。**

占空比 ≈ 飞轮转速，而力矩 ∝ 转速的导数，所以：

| 占空比里的项 | 飞轮转速 | 力矩 ∝ | 作用 |
|---|---|---|---|
| ∝ 角速度 | ∝ 角速度 | 角加速度 | 改变等效惯量 |
| ∝ 角度 | ∝ 角度 | **角速度** | **阻尼** |
| **∝ ∫角度** | ∝ ∫角度 | **角度** | **回正** |

**注意第二行**：占空比正比于角度给出的是阻尼，不是回正。所以一条纯 P 的串级
（内环 P + 角度环 P）在飞轮上只有阻尼，回正力矩恒等于 0 ——
表现就是"阻尼感很强、不抖，但就是立不起来，推回去只有一点点力气"。

判据：看 `roll` 波形里**飞轮转速跟谁同相**。
跟角度同相 = 纯阻尼，没有回正；要有回正，转速必须落后角度 90°。

**回正项就是 `r_angle_ki`**（角度环积分 → 角速度命令 → 占空比含 ∫角度）。
参考独轮工程比赛版用的 `KI·∫角度` 给了满量程 28% 的权限，不是可选项。

**四、静态倾角撑不了多久，飞轮必然饱和。**

占空比一路爬到限幅，飞轮就到极速不再加速，力矩再次归零，车这时才真的倒。
这不是能靠调增益消除的，是反作用轮的物理限制。回收环 `r_rcy` 就是干这个的：
它把飞轮转速拉回 0 换回可用力矩，代价是车体要稍微歪一点。

### 开了 Angle 环，歪着飞轮还是匀速转 —— 按这个顺序查

Angle 环开着、车静止歪着，**飞轮应该越转越快**（占空比一路往上爬）。
如果是匀速，一定是某处把"每拍继续加"这条路断了。

**先看 `roll` 波形的 CH6（最终输出）和 CH4（角度环输出），一眼就能分出是哪一类：**

| CH6 | CH4 | 说明 | 查哪里 |
|---|---|---|---|
| 恒定不动 | 恒为 0 | 角度环没进来 | 是不是按的 `Start Rate Test`？Rate 模式下 `r_angle_pid` 被 `pid_reset()` 按住 |
| 恒定不动 | 恒为 0 | 角度环进来了但没输出 | `r_angle_kp` 还是 0，默认值就是 0 |
| 恒定不动 | 非 0 | 内环没有积分路 | `r_rate_ki` 还是 0，只有 kp 那一路。kp 累加后等于 `kp·角速度`，静止时恒定 |
| 顶在 ±10000 | 非 0 | 已经饱和 | 到 `BAL_TEST_FLY_LIMIT` 了，`pid_inc_calc_limited()` 的抗饱和会把 `out_i` 清 0，爬不动了 |
| 在爬 | 非 0 | **正常** | 转速也应该在涨；转速不涨就查 `Dir A/B` 和转速回读 |

按发生概率排：**`r_angle_kp` 还是 0 → 按的是 Rate 不是 Angle → `r_rate_ki` 还是 0**。
三个默认值都是 0，只要有一个没填，现象就都是"匀速"。

## 8. 两条不能忘的

**`angle_kp` 要给得比直觉小一到两个数量级。** 内环的 ki 那一路本身已经是角度反馈了（第 4 节），
内环单独就能站住。角度环叠上去只负责消掉静态倾角，给大了立刻冲饱和。

**Rate 和 Angle 不能长时间挂着。** 这两个模式下飞轮回收环是断开的，只要输出停在非零值上，
飞轮就一路加速到极速，触发驱动的堵转保护。**那个保护是闩锁的，不重启驱动不恢复**，
之后整个上电周期飞轮都是死的。所以这两个模式只能点一下看响应就停。
`Fly RpmMx`（默认 7000）是软保护，超了自动停测试并显示 `FLYWHEEL OVERSPEED`。能长时间跑的只有 `Speed`。

## 9. 分轴调

### 顺序，以及"调 Pitch 的时候 Roll 要不要一起跑"

```text
Roll Rate → Roll Angle          只放开横滚的架子
Pitch Rate → Pitch Angle        只放开俯仰的架子
Pitch Speed → Roll Speed        还在架子上
Yaw Rate → Yaw Angle            架子上或者手扶
Balance                         三轴一起，落地，只做微调
```

**单轴阶段靠架子解耦，不靠让别的轴一起跑。**

调 Pitch 时 A/B 是刹车锁死的（`test_drive_c_output()` 里先 `W_Motor_Stop()` 再驱动 C），
这是有意的，不是漏了：

- 两个轴同时闭环，车一倒你分不清是 Pitch 的增益不对还是 Roll 的增益不对，波形上也分不出来。
- 单轴 Test 里回收环是旁路的。Roll 只要有一点单向输出，整段 Pitch 测试期间飞轮都在往饱和跑，
  没有任何东西把它拉回来。
- Roll 本身还没调好的时候让它参与，只会让车更难站，不会更好调。

所以调 Pitch 需要的是**一个只放开俯仰自由度的架子**（前后能倒，左右被夹住），
不是把 Roll 打开。调 Roll 反过来。

**实在没有架子怎么办**：先把 Roll Rate / Roll Angle 单独调完，然后直接进 `Balance`，
在三轴一起跑的状态下用串口调 `p_rate_ki` → `p_rate_kp` → `p_angle_kp`。
代价是从 0 起调的那几步一定会摔车，所以起手值要按第 5 节的表填进去再进 Balance，别从 0 开始。

### Roll Rate

`Params → Roll → Start Rate Test`

`r_rate_ki` 从 **0.1** 起，每次翻倍，落点通常在 **0.2 ~ 1.0**。手推车体往一侧倒。

| 现象 | 说明 | 下一步 |
|---|---|---|
| 车顺着手甩出去 | 符号反了 | 翻 `r_rate_ki` 符号 |
| 完全没反应 | ki 还太小 | 翻倍，加到 2 还没反应就别加了，去查波形符号 |
| 推着费劲，松手后来回晃不停 | ki 到位了 | 停下来加 `r_rate_kp` |
| 推着费劲，松手后晃两下停 | **合格** | 进 Angle |
| 高频抖、听得见啸叫 | kp 大了 | `r_rate_kp` 减半 |

`r_rate_kp` 从 **5** 起翻倍，落点通常在 **10 ~ 80**，超过 200 不要再加。

合格标准：手推能明显感到顶回来，松手 1 秒内停止摆动，飞轮不持续单向加速。

**这一环只判动态，不判静态。**慢慢把车压到一个角度按住不动，飞轮会稳到匀速、不再顶你 ——
那是对的，不是没调好，原因见第 7 节。静态回正是下一步 Angle 环的验收项。

### Roll Angle

`Start Angle Test`

`r_angle_kp` 从 **1** 起翻倍，落点通常在 **2 ~ 20**，超过 50 基本必振荡。

合格标准：**手把车压到一侧再松手，能自己回到横滚零点**，静态偏差小于 2°，不来回摆。
按住不放的时候飞轮应该越转越快 —— 这一条是 Angle 环有没有真正接进来的判据，
匀速就说明没接进来，按第 7 节末尾那张表查。

一直偏向同一侧先去 `Params → Zero` 重标零点，别靠加大 `r_angle_kp` 硬拉。

### Pitch Rate

`Params → Pitch → Start Rate Test`

`p_rate_ki` 从 **0.05** 起翻倍，落点通常在 **0.1 ~ 0.8**，超过 2 不要再加。
车体前倾时，C 轮应该往前追重心。

方向不对先回 Motor 页查 `Dir C` 和 `Enc C`，别在这里翻符号。

`p_rate_kp` 从 **2** 起翻倍，落点通常在 **5 ~ 50**，超过 150 不要再加。

合格标准：前后推车体都有明显反抗，松手后不持续点头。

> 台架限幅 `BAL_TEST_DRIVE_LIMIT` 是 8000，和实跑的 `DRIVE_OUT_LIMIT` 一样，
> 所以 Pitch 这一轴台架整定完的值可以直接用。

### Pitch Angle

`Start Angle Test`

`p_angle_kp` 从 **1** 起翻倍，落点通常在 **2 ~ 20**，超过 50 不要再加。

合格标准：回到俯仰零点，静态偏差小于 2°。

Roll 和 Pitch 的 Angle 都过了，去 `Params → Zero → Capture Zero` 抓零点，在 Zero 页按 Save。

> 每一页的 `Save` 只存那一页的参数。串口改完 `r_rate_ki` 要去 Roll 页按 Save，
> 在别的页按存不进去。想一次存全部就用 `Params` 最底下那个 Save。

### Pitch Speed

`Start Speed Test`

目标速度固定 0，只能调"停住不溜车"。

`p_vel_kp` 从 **0.005** 起翻倍，落点通常在 **0.005 ~ 0.05**，超过 0.2 不要再加。
判断标准直接看倾角：`p_vel_kp × 当前 counts/20ms` 就是它命令的倾角，超过 3° 就是大了。

合格标准：车轻微溜出去之后能自己回来，`ENC C` 长期不单向累加。

非零速度和速度斜坡等 Run 做完再说。

### Roll Speed

`Start Speed Test`

这一环把飞轮转速拉回 0。

先确认 `Rcy Tau` = 2.0（默认值，别关）。
`r_rcy_kp` 从 **0.002** 起翻倍，落点通常在 **0.001 ~ 0.01**，超过 0.1 不要再加。

同样按倾角判：`r_rcy_kp × 飞轮转速差(RPM)` 就是它命令的倾角，超过 3° 就是大了。
`Rcy Lim`（`r_rcy_limit`，默认 3°）把这个命令硬夹住，先别动它。

合格标准：**能连续挂 30 秒以上不触发 `FLYWHEEL OVERSPEED`**，飞轮转速在 0 附近来回，不单向堆积。

加太大车体刚度会变软，推一下倒得比之前快。

#### 一开回收环就抖 —— 先看 `Rcy Tau`，不要去加硬内环

**最常见的原因是回收环的反馈没滤波，它跟着平衡环的节奏在动。**

飞轮差速里同时有两样东西：回收环要纠正的**秒级直流漂移**，和平衡环干活产生的**交流纹波**。
实测纹波幅值是直流漂移的**十几倍**。不滤掉的话回收环会把纹波反号灌回角度环误差，
零点跟着 2Hz 抖，车就跟着抖 —— 这时候**减小 `r_rcy_kp` 是没用的**，两者比例摆在那，
能纠正直流的增益对交流一定过大。

所以先确认 `Rcy Tau`（`r_rcy_tau`，默认 2.0 s）不是 0，抖就往上加到 3。
2 s 的时间常数对 2 Hz 纹波衰减约 25 倍，对直流漂移完全不衰减 —— 回收本来就是秒级的慢事。

内环加硬是另一个方向的误区。**内环已经过了它自己的验收，问题不在内环。**
内环加硬只会让它更快、更准地去追那个本来就在乱跳的目标，抖得更厉害。

回收环是这条串级里最外、最慢、反馈最脏的一环：

- 它的输出是**零点偏移**，角度环会老老实实把车歪过去。这一路一跳，整个车就跟着晃。
- 反馈是霍尔六步回传的转速差，低速时是**阶梯值**，20ms 才更新一次，还可能是 10ms 前的。
  阶梯直接乘 `kp` 就是一串台阶状的零点命令。
- 更根本的是，飞轮减速必须靠车体歪一点、让重力做功。回收环和平衡环天生在互相拉扯，
  所以它**必须比角度环慢得多**，快了一定打架。

按这个顺序查，看 `roll` 波形：

| 现象 | 是什么 | 怎么办 |
|---|---|---|
| CH2（回收环输出）在 ±`Rcy Lim` 之间来回顶、频率和车抖一样 | **反馈没滤干净**，回收环在跟平衡环共振 | **`Rcy Tau` 加到 3**，这一条最常见 |
| CH1（滤波后反馈）还在跟着 CH3（横滚角）同频摆 | 同上，时间常数不够 | 继续加 `Rcy Tau` |
| CH1 平稳但 CH2 仍顶在限幅 | 直流漂移确实大，增益过头 | `r_rcy_kp` 减半 |
| CH2 基本平，CH5/CH6 高频毛刺、有啸叫 | 这才是内环的事 | `r_rate_kp` 减半 |

前三种都不要动内环。只有最后一种才回去改 `r_rate_kp`，而且是**减小**不是加大。

判据很简单：**CH2 的抖动频率如果和车抖的频率一样，那就是滤波不够，不是增益问题。**

顺带确认 `r_rcy_ki` 和 `r_rcy_kd` 确实是 0 —— 对一串台阶做微分，每个台阶都会发一根尖刺。

### Yaw Rate

`Params → Yaw → Start Rate Test`

**这一环是位置式，只调 `kp`。**`y_rate_kp` 从 **2** 起翻倍，落点通常在 **5 ~ 50**，超过 100 不要再加。
`y_rate_ki` 保持 0——它的 `imax` 是 100，误差稍大积分器一两拍就顶死，给了只会抖。

手扭车体。合格标准：扭动时有明显反向阻尼，松手不自转。

### Yaw Angle

`Start Angle Test`

启动那一瞬间的朝向就是目标，测试本身不会主动给航向阶跃。

`y_angle_kp` 从 **0.5** 起翻倍，落点通常在 **1 ~ 5**，超过 20 不要再加。

合格标准：手扭偏 30° 松手后能转回启动朝向，超调小于 10°。

## 10. Balance：三轴一起跑

八个环全部单独过关之后，回主菜单最后一行 `Balance: OFF`。

**连按两次确认才启动**：第一次显示 `PRESS AGAIN TO BALANCE`，3 秒内再按一次才真的松飞轮刹车。
跑起来后那一行变成 `Balance: ON`，再按一次是停，返回键在任何页面都能立刻停。

启动条件比单轴 Test 多一条：**起步时车必须已经扶到保护角以内**，否则直接拒绝并报
`ROLL OVER PROTECT` / `PITCH OVER PROTECT`。倒着按下去等于松刹车瞬间给一个满幅冲击，
而且下一拍保护角照样会把它停掉。

和单轴 Test 的区别：

| | 单轴 Test | Balance |
|---|---|---|
| 闭环 | 只有一个轴，更外的环旁路清零 | 八个环全在环里 |
| 速度目标 | 0 | 0（原地平衡，视觉和转向命令都不接） |
| 回收环 | Rate/Angle 时是断的 | 一直在 |
| 串口调参 | 只放行当前轴、当前最高启用环 | **24 个全放行**，边跑边改 |
| 波形 | `roll` / `pit` / `yaw` | `bal` |
| 翻页 | 停 | **不停**，波形也不断 |

安全闸和单轴 Test 同一套，另外多一条飞轮软超速：任何一条触发都会打回 STOP 并锁死刹车，
菜单上直接显示是哪一条停的（`FLYWHEEL OVERSPEED`、`BLDC LINK LOST`、`ROLL OVER PROTECT` …）。

合格标准：

- 松手能自己站住 30 秒以上，不靠人扶。
- `bal` 波形 CH0 贴着 CH1、CH2 贴着 CH3，偏差小于 2°。
- **CH8 和 CH9（A/B 转速）在 0 附近来回，不同向一起爬升。**一起爬就是回收环压不住，
  再跑下去要么触发软超速保护，要么闩死驱动的堵转保护。
- CH10（C 轮 counts/20ms）长期不单向累加，车不溜。

调不动的时候回去看是哪一轴先垮：CH0 先跑掉就回 Roll 页，CH2 先跑掉就回 Pitch 页。
在 Balance 里只做微调，从 0 起调那种量级的改动回单轴 Test 做。

## 11. 波形怎么看

无线转串口模块，UART2 @115200。模块 RX 接 `P10_5`、TX 接 `P10_6`、RTS 接 `P10_2`。约 20 ms 一帧。

| 模式 | 通道 |
|---|---|
| `att` | CH0 Roll，CH1 Pitch，CH2 Yaw |
| `roll` | CH0回收目标，CH1回收反馈（**低通之后**，原始差速用 CH10−CH9 反算），CH2回收输出，CH3 Roll角，CH4角度环输出，CH5 Roll角速度，CH6最终输出，CH7/8 A/B指令，CH9/10 A/B转速，**CH11 r_rate_kp，CH12 r_rate_ki，CH13 r_angle_kp，CH14 r_rcy_kp** |
| `pit` | CH0速度目标，CH1速度反馈，CH2速度环输出，CH3 Pitch角，CH4角度环输出，CH5 Pitch角速度，CH6最终输出，**CH7 p_rate_kp，CH8 p_rate_ki，CH9 p_angle_kp，CH10 p_vel_kp** |
| `yaw` | CH0航向目标，CH1连续航向，CH2外环输出，CH3 Yaw角速度，CH4最终输出，CH5压弯偏移，**CH6 y_rate_kp，CH7 y_angle_kp** |
| `mot` | CH0/1 A/B指令，CH2/3 A/B转速，CH4 C轮20ms计数 |
| `bal` | CH0 Roll角，CH1 Roll目标，CH2 Pitch角，CH3 Pitch零点，CH4 航向误差，CH5/6/7 A/B/C指令，CH8/9 A/B转速，CH10 C轮20ms计数，**CH11 r_rate_kp，CH12 r_rate_ki，CH13 p_rate_kp，CH14 p_rate_ki** |

**每种模式末尾那几路发的是当前正在用的增益本身。**改一次参数波形上跟着跳一个台阶，
录下来的 CSV 自带增益信息，回放时不用猜哪一段对应哪次改动，也不用事后凭记忆对。

只开 Rate 时角度环被旁路清零，`roll` 的 CH0~CH2、CH4 恒为 0，只有 CH3、CH5、CH6 在动。

**判极性只看两条线：CH5（角速度）和 CH6（输出）。**

推一下车体，CH5 出一个尖峰，CH6 应该跟着**同号**顶起来。反号就是增益符号错了。

看车更直接：飞轮产生的力矩应该**阻碍**你推的方向。推着费劲 = 对；顺着手甩出去 = 反了。

Angle 环同理：CH3（角度）、CH4（角度环输出）、CH6 三条同号。

混控是 `A = -roll + yaw`、`B = +roll + yaw`，所以 CH6 为正时 B 正转、A 反转。

**外环 kp 大不大，看 CH2（外环输出）就够了。**
`roll` 的 CH2 和 `pit` 的 CH2 单位都是度，长期超过 3° 就是那一环给大了，不用等车表现出来。
