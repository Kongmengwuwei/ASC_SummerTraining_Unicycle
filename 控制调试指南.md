# 控制调试指南

`Run` 还是占位。下面只讲架空点动和单轴 Test，三轴发车入口做完之前不许落地。

## 1. 调之前

1. 车架空，手边留断电开关。
2. 上电别碰车，等主菜单出 `IMU READY`。
3. Roll/Yaw 还要等 `BLDC UART OK`。

主菜单出这几条就别开测试：

```text
IMU INIT FAILED / IMU LINK LOST / IMU CALIB MOVED
ATTITUDE CONVERGING / ATTITUDE DIVERGED / BLDC LINK LOST
```

编译默认 PID 全是 0，但 Flash 里存过的值会盖掉它。先去菜单看当前值。

## 2. Motor 点动，先把方向定死

`Params → Motor`，六个动作行依次点，再按一次同一行停。屏幕下面三行同时看：

```text
AIRBORNE JOG A CMD +2500
RPM  A +1200 B     0
ENC  C  +180 TOT   +24500
```

| 查什么 | 合格标准 | 不对怎么改 |
|---|---|---|
| A/B 指令与转速同号 | `CMD +2500` 配 `RPM A +1200` | 翻 `Dir A` / `Dir B` |
| C 正转是车往前 | 车往前走 | 翻 `Dir C` |
| C 编码器与转向同号 | 往前走 `ENC C` 为正 | 翻 `Enc C` |

**方向只在这一页定，定完就别再动了。** 后面所有极性问题都在 PID 增益的符号上解决。

原因：`Dir A/B` 同时作用于占空比下发和转速回读。回头翻它，转速回读跟着翻，飞轮回收环会从负反馈变成正反馈 —— 车能站住几秒然后突然甩飞。

点动不跑姿态闭环，也不查保护角，只能架空，随时准备按返回键。

## 3. Test 规则

| 动作行 | 开了哪几个环 |
|---|---|
| Rate | 只开角速度环 |
| Angle | 角速度环 + 角度环 |
| Speed | 再开最外环，Roll 是回收环，Pitch 是速度环 |

Yaw 没有 Speed。

启动时清 PID、清输出、目标航向对齐当前 Yaw。测试期间轴和最高环锁定，串口只能改当前已启用范围内的 PID。按返回键随时停。

## 4. 三条必须先知道的

**内环先给 ki，不是先给 kp。** `r_rate` / `p_rate` / `y_rate` 三个内环是增量式，`ki` 才是把车扶起来的那一路，`kp` 是阻尼。`ki` 给 0 的话电机基本不动，车只会慢慢倒。顺序一律是 ki → kp，`kd` 保持 0。

**`angle_kp` 要给得比直觉小一到两个数量级。** 内环单独就已经是一套完整的角度控制，角度环叠上去只负责消掉静态倾角。给大了立刻冲饱和。`angle_ki` 保持 0。

**Rate 和 Angle 不能长时间挂着。** 这两个模式下飞轮回收环是断开的，只要输出停在非零值上，飞轮就一路加速到极速，触发驱动的堵转保护。**那个保护是闩锁的，不重启驱动不恢复**，之后整个上电周期飞轮都是死的。所以这两个模式只能点一下看响应就停。`Fly RpmMx`（默认 4000）是软保护，超了自动停测试并显示 `FLYWHEEL OVERSPEED`。能长时间跑的只有 `Speed`。

## 5. 分轴调

顺序：Roll Rate → Roll Angle → Pitch Rate → Pitch Angle → Pitch Speed → Roll Speed → Yaw Rate → Yaw Angle。

### Roll Rate

`Params → Roll → Start Rate Test`

`r_rate_ki` 从 0.05 开始往上加，每次加一倍。手推车体往一侧倒。

| 现象 | 说明 | 下一步 |
|---|---|---|
| 车顺着手甩出去 | 符号反了 | 翻 `r_rate_ki` 符号 |
| 推着费劲，松手后来回晃不停 | ki 到位了 | 加 `r_rate_kp` |
| 推着费劲，松手后晃两下停 | **合格** | 进 Angle |
| 高频抖、听得见啸叫 | kp 大了 | `r_rate_kp` 减半 |

合格标准：手推能明显感到顶回来，松手 1 秒内停止摆动，飞轮不持续单向加速。

### Roll Angle

`Start Angle Test`

`r_angle_kp` 从 1 开始，每次加一倍。

合格标准：车体停在横滚零点附近，静态偏差小于 2°，不来回摆。

一直偏向同一侧先去 `Params → Zero` 重标零点，别靠加大 `r_angle_kp` 硬拉。

### Pitch Rate

`Params → Pitch → Start Rate Test`

`p_rate_ki` 从 0.01 开始。车体前倾时，C 轮应该往前追重心。

方向不对先回 Motor 页查 `Dir C` 和 `Enc C`，别在这里翻符号。

合格标准：前后推车体都有明显反抗，松手后不持续点头。

### Pitch Angle

`Start Angle Test`

`p_angle_kp` 从 1 开始。

合格标准：回到俯仰零点，静态偏差小于 2°。

Roll 和 Pitch 的 Angle 都过了，去 `Params → Zero → Capture Zero` 抓零点，然后 Save。

### Pitch Speed

`Start Speed Test`

目标速度固定 0，只能调"停住不溜车"。`p_vel_kp` 从 0.005 开始。

合格标准：车轻微溜出去之后能自己回来，`ENC C` 长期不单向累加。

非零速度和速度斜坡等 Run 做完再说。

### Roll Speed

`Start Speed Test`

这一环把飞轮转速拉回 0。`r_rcy_kp` 从 0.05 开始。

合格标准：**能连续挂 30 秒以上不触发 `FLYWHEEL OVERSPEED`**，飞轮转速在 0 附近来回，不单向堆积。

加太大车体刚度会变软，推一下倒得比之前快。`r_rcy_kd` 保持 0 —— 霍尔六步低速回传是阶梯值，微分全是台阶噪声。

### Yaw Rate

`Params → Yaw → Start Rate Test`

`y_rate_ki` 从 0.02 开始。手扭车体。

合格标准：扭动时有明显反向阻尼，松手不自转。

### Yaw Angle

`Start Angle Test`

启动那一瞬间的朝向就是目标，测试本身不会主动给航向阶跃。`y_angle_kp` 从 0.5 开始。

合格标准：手扭偏 30° 松手后能转回启动朝向，超调小于 10°。

## 6. 波形怎么看

无线转串口模块，UART2 @115200。模块 RX 接 `P10_5`、TX 接 `P10_6`、RTS 接 `P10_2`。约 20 ms 一帧。

| 模式 | 通道 |
|---|---|
| `att` | CH0 Roll，CH1 Pitch，CH2 Yaw |
| `roll` | CH0回收目标，CH1回收反馈，CH2回收输出，CH3 Roll角，CH4角度环输出，CH5 Roll角速度，CH6最终输出，CH7/8 A/B指令，CH9/10 A/B转速 |
| `pit` | CH0速度目标，CH1速度反馈，CH2速度环输出，CH3 Pitch角，CH4角度环输出，CH5 Pitch角速度，CH6最终输出 |
| `yaw` | CH0航向目标，CH1连续航向，CH2外环输出，CH3 Yaw角速度，CH4最终输出，CH5压弯偏移 |
| `mot` | CH0/1 A/B指令，CH2/3 A/B转速，CH4 C轮20ms计数 |

只开 Rate 时角度环被旁路清零，`roll` 的 CH0~CH2、CH4 恒为 0，只有 CH3、CH5、CH6 在动。

**判极性只看两条线：CH5（角速度）和 CH6（输出）。**

推一下车体，CH5 出一个尖峰，CH6 应该跟着**同号**顶起来。反号就是增益符号错了。

看车更直接：飞轮产生的力矩应该**阻碍**你推的方向。推着费劲 = 对；顺着手甩出去 = 反了。

Angle 环同理：CH3（角度）、CH4（角度环输出）、CH6 三条同号。

混控是 `A = -roll + yaw`、`B = +roll + yaw`，所以 CH6 为正时 B 正转、A 反转。

## 7. 停机了看这个

停机时屏幕状态行会写具体原因：

| 状态行 | 原因 | 怎么办 |
|---|---|---|
| `ROLL OVER PROTECT` | 横滚超 `Roll Prot` | 调大 `Params → Zero → Roll Prot` 或重标零点 |
| `PITCH OVER PROTECT` | 俯仰超 `Pitch Prot` | 台架侧倾常带出俯仰，调大 `Pitch Prot` 或换台架 |
| `FLYWHEEL OVERSPEED` | 飞轮超 `Fly RpmMx` | 输出停在非零值跑飞了，减小增益或接上 Speed 环 |
| `ATTITUDE NOT READY` | 姿态没收敛 | 上电静置等收敛 |
| `IMU CALIB INVALID` | 上电标定期间车动过 | 断电静置重新上电，中途别碰车 |
| `ATTITUDE DIVERGED` | 四元数发散或姿态 NaN | 查 IMU 供电和 SPI |
| `IMU LINK LOST` | IMU 读数停更 | 查 SPI0 接线 |
| `BLDC LINK LOST` | UART3 断链，只影响 Roll/Yaw | 查 `P15_6/P15_7`，Pitch 不受影响 |

保护角判的是**偏离机械零点**的量，不是绝对角。零点没标好会让保护角提前触发。

**驱动 LED 慢闪 = 已经进保护，闩锁的，必须重启驱动**，光停测试没用。原因是失控保护（500ms 没收到占空比指令）或堵转保护（三相线接触不良 / 飞轮跑到极速）。

## 8. 通用规矩

- 一次只改一个参数。
- 每一步都有明确的合格标准，没达到别往下走。
- 稳定了再 Save，不 Save 掉电就没了。
- 测完确认三路输出为 0、A/B 刹车锁定，再断电。
- 别抄别人的 PID 数值。
