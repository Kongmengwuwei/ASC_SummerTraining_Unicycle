# 控制调试指南

当前 `Run` 仍是占位。以下步骤只用于架空点动和单轴 Test/Wave；完整三轴发车入口完成前，不允许自由落地。

## 1. 控制结构

| 轴 | 串级 | 执行机构 |
|---|---|---|
| Roll | 回收环 20 ms → 角度环 5 ms → 角速度环 1 ms | A/B 差动 |
| Pitch | 速度环 20 ms → 角度环 5 ms → 角速度环 1 ms | C 轮 |
| Yaw | 航向外环 5 ms → 角速度内环 1 ms | A/B 同向 |

C 轮编码器每 5 ms 读取一次，累计成 `counts/20ms` 供速度环使用。

Roll/Pitch 角速度环使用增量式 PID，累计输出由最终输出限幅约束。角度环、速度/回收环和 Yaw 两环使用位置式 PID。

## 2. 测试前检查

1. 把车可靠架空，手边保留断电开关。
2. 上电时保持静止，等待 `ATTITUDE READY` 和标定通过。
3. Roll/Pitch/Yaw 方向与实际车体一致。
4. Roll/Yaw 测试前确认 `BLDC READY`。
5. 返回键、保护角和驱动失控保护必须有效。

出现以下状态时不要测试：

```text
IMU INIT FAILED
IMU LINK LOST
CALIB MOVED / REBOOT
ATTITUDE CONVERGING
ATTITUDE DIVERGED
BLDC LINK LOST
```

PID 编译默认值全为 0，但有效 Flash 参数会覆盖默认值。调参前先看菜单或 `list` 的当前值。

## 3. Motor 点动

进入 `Params → Motor`，依次点动 A/B/C 正反转。

- A/B 指令和回传转速的正负应一致。
- C 正方向应与车辆前进方向一致。
- C 编码器正方向错误时改 `Enc C`。
- 单次点动 1500 ms，自动切到 `mot` 波形并在结束后关闭。

点动只用于验方向，不运行姿态闭环。

## 4. Test/Wave 规则

| 模式 | 启用范围 |
|---|---|
| Rate | 只启用角速度环 |
| Angle | 角速度环 + 角度环 |
| Speed | 再启用最外环；Roll 为回收环，Pitch 为速度环 |

Yaw 只有 Rate 和 Angle。

启动 Test 时会清 PID、输出和速度目标，并把目标航向对齐当前 Yaw。测试期间轴和最高环锁定，只允许修改当前已启用串级内的 PID。

公共调参原则：

1. 先确认方向，正反馈时立即停机。
2. P 从小到大建立响应。
3. 有振荡再少量加 D 或降低 P。
4. I 最后添加且尽量小。
5. 一次只改一个参数，稳定后再 Save。

不要照抄其他车的 PID 数值。

## 5. 调试顺序

### Roll Rate

`Params → Roll → Start Rate Test/Wave`

手动左右转动车体，应出现反向阻尼。越推越倒说明电机、IMU 或误差符号错误。

### Roll Angle

`Start Angle Test/Wave`

目标是车体能回到横滚机械零点。固定偏向一侧时先检查结构和零点，不要先堆积分。

### Pitch Rate

`Params → Pitch → Start Rate Test/Wave`

车体向前倾时，C 轮应向前追赶重心。方向错误先检查 `Dir C`、`Enc C` 和 Pitch 符号。

### Pitch Angle

`Start Angle Test/Wave`

目标是回到俯仰机械零点，不持续前后点头。Roll/Pitch 角度环基本可用后，进入 `Zero → Capture Zero`，再 Save。

### Roll Speed

`Start Speed Test/Wave`

该环把 A/B 差动转速拉回 0，防止飞轮长期单向累积。回收过强会削弱 Roll 刚度。霍尔回传低速呈阶梯状，`r_rcy_kd` 先保持 0。

### Pitch Speed

`Start Speed Test/Wave`

当前测试目标固定为 0，只能调零速度恢复，不能测试非零行驶速度。非零速度和速度斜坡留到 Run 实现后。

### Yaw Rate

`Params → Yaw → Start Rate Test/Wave`

手动扭动车体，应有反向转动阻尼。

### Yaw Angle

`Start Angle Test/Wave`

启动瞬间的当前方向就是目标。手动扭偏后，观察是否回到该方向；测试本身不会主动给航向阶跃。

## 6. Yaw 基准

`imu_get_angle_yaw()` 是连续相对航向，不做 ±180° 包装，也不是绝对航向。六轴 IMU 没有绝对航向参考，长期会漂移。

每次 Test 启动都会执行：

```c
g_yaw_target = imu_get_angle_yaw();
```

正常执行三轴串级时，STOP 和 DRIVE_ONLY 会持续让目标航向跟随当前 Yaw；单轴 Test 启动后则锁定启动瞬间的 Yaw。这样启用闭环时初始航向误差为 0，不会让 A/B 飞轮突然猛转。

不要把连续 Yaw 强制清零。环岛使用独立累计转角。未来 Run 入口仍应在状态切换瞬间再次对齐当前 Yaw 并清 Yaw PID。

## 7. VOFA 波形

UART0：115200，默认约 20 ms 一帧。

| 模式 | 通道 |
|---|---|
| `att` | CH0 Roll，CH1 Pitch，CH2 Yaw |
| `roll` | CH0回收目标，CH1回收反馈，CH2回收输出，CH3 Roll角，CH4角度环输出，CH5 Roll角速度，CH6最终输出 |
| `pit` | CH0速度目标，CH1速度反馈，CH2速度环输出，CH3 Pitch角，CH4角度环输出，CH5 Pitch角速度，CH6最终输出 |
| `yaw` | CH0航向目标，CH1连续航向，CH2外环输出，CH3 Yaw角速度，CH4最终输出，CH5压弯偏移 |
| `mot` | CH0/1 A/B指令，CH2/3 A/B转速，CH4 C轮20 ms计数 |

测试由菜单启动，UART0 当前没有发车或停车命令。活动测试中 `axis`、`ring`、`wave` 和 `save` 会被拒绝。

## 8. 单轴 Test/Wave 停机条件

- IMU 初始化、标定、链路或姿态异常。
- Roll/Pitch 超过保护角。
- Roll/Yaw 测试期间 CYT2BL3 断链。
- 按返回键停止当前测试。

测试完成后确认三路输出为 0、A/B 刹车锁定，再保存参数或断电。

Motor Jog 运行中只按 1500 ms 倒计时结束；A/B 点动还会检查 CYT2BL3 断链。点动开始后不会持续执行 IMU、姿态角保护，因此只能架空并随时准备按返回键。
