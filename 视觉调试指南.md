# 视觉调试指南

当前视觉和元素状态机运行在 CPU1。结果会显示并发送波形，但不会改变目标航向、目标速度或电机输出；主菜单 `Run` 仍未启用。

## 1. 当前链路

```text
MT9V03X 188×120
→ VSYNC 启动 DMA
→ DMA 完成发布新帧
→ 复制底部居中 180×80 ROI
→ 大津二值化
→ 图像滤波
→ 八邻域提取左右边线
→ 标准赛宽补中线
→ 计算基础偏差
→ 元素检测、补线或单边处理
→ 重建中线并计算最终偏差
→ 发布给 CPU0
```

ROI 为原图列 `4~183`、行 `40~119`。算法图行 0 是远端，行 79 是近端。

摄像头使用单原图缓冲。CPU1 尚未复制上一帧时，新 VSYNC 会丢弃整帧，避免 DMA 覆盖正在读取的数据。

## 2. Image 页

主菜单进入 `Image` 后自动开启 `trk` 波形并切为横屏。

| 按键 | 功能 |
|---|---|
| 确认 | `BIN+L → BIN → GRAY` 循环 |
| 上/长按上 | 曝光增加 16 |
| 下/长按下 | 曝光减少 16 |
| 返回 | 退出并恢复竖屏 |

180×80 图像按比例显示为 320×142。

状态栏：

- `EXP`：请求的曝光值。
- `TH`：本帧大津阈值。
- `OK`：本帧 `Track_Valid`。
- `EL`：当前元素编号。

`BIN+L` 颜色：

- 绿：左边线。
- 蓝：右边线。
- 红：有效中线，包括单边赛宽补偿。
- 黄：双边都丢失后沿用的中线。

## 3. 基础视觉调试

1. 固定摄像头高度、俯角和支架。
2. 在 `GRAY` 调曝光，使黑线和白色赛道有明显灰度差。
3. 切到 `BIN`，确认黑白区域稳定且碎点少。
4. 切到 `BIN+L`，确认左右边线贴合，中线连续。
5. 进入 `Params → Camera → Start Vision Test/Wave` 看 `ERR` 和 `trk` 波形。
6. 车放在直道中央，调整 `Error Zero`，使 `ERR` 接近 0。
7. Save 后断电复查参数是否保持。

误差定义：

```text
err = 图像中心列 - 中线列
```

中线在图像左侧时 `err > 0`，在图像右侧时 `err < 0`。

`ROAD_WIDE_NEAR=133`、`ROAD_WIDE_FAR=30` 生成 `Standard_Road_Wide[]`。它既用于单边补中线，也参与环岛单边偏差、坡道超宽、路障收窄与恢复判断，必须按最终摄像头位置标定；改动后要重新核对这些元素阈值。当前页面不显示逐行赛宽，精确标定需临时增加调试量或离线读图。

## 4. 元素编号与作用

| EL | 元素 | 当前处理 |
|---:|---|---|
| 0 | 无 | 普通中线 |
| 1 | 斑马线 | 发布 `stop_request=1` |
| 2 | 十字 | 按角点延长边线 |
| 3 | 左环岛 | 编码器与独立转角状态机、单边偏差 |
| 4 | 右环岛 | 编码器与独立转角状态机、单边偏差 |
| 5 | 坡道 | 发布坡道速度倍率 |
| 6 | 路障 | 按赛宽收窄偏移边线 |

这些输出当前只用于调试。Run 未接入，所以不会停车、减速、转向或绕障。

元素仍是初版：

- 阈值未实车标定。
- 环岛没有状态超时。
- 坡道目前只用图像判据，尚未融合 Pitch。
- 路障只用图像赛宽，没有 TOF。
- 多数候选缺少完整的多帧确认和触发屏蔽。

先调通普通直道和弯道，再逐项验证元素。

## 5. VOFA `trk`

UART0：115200，FireWater。

| 通道 | 内容 |
|---:|---|
| 0 | 中线偏差 |
| 1 | `Track_Valid` |
| 2 | `Search_Stop_Line` |
| 3 | 左丢线行数 |
| 4 | 右丢线行数 |
| 5 | 双丢线行数 |
| 6 | 大津阈值 |
| 7 | 连续无效帧数 |
| 8 | CPU1 心跳 |
| 9 | 距上一帧结果的时间，ms |
| 10 | 元素编号 |
| 11 | 元素建议速度倍率 |
| 12 | 元素停车请求 |

心跳变化只说明 CPU1 主循环在运行。判断摄像头是否持续出帧，主要看 CH9 是否周期回到较小值。

## 6. 当前限制

- 视觉偏差尚未写入 `g_yaw_target`。
- 元素速度倍率和停车请求尚未进入控制层。
- 没有无线图传；图像只在 IPS200 显示。
- 大津阈值没有跨帧滤波。
- ROI、前瞻和标准赛宽固定，没有逆透视或动态前瞻。
- `Search_Stop_Line` 最大为 78。
- Image 页可能保留最后一帧；停帧时看主菜单 CAM 状态或 `trk` CH9。

## 7. 常见故障

| 现象 | 优先检查 |
|---|---|
| `CAMERA INIT FAILED` | 电源、UART1 `P02_2/P02_3`、VSYNC `P02_0`、PCLK `P02_1`、D0-D7 |
| 长时间 `CAMERA STARTING` | CPU1、摄像头配置通信 |
| 心跳变化但 CH9 持续增加 | VSYNC、PCLK、DMA5、DMA 完成中断 |
| 全黑或全白 | 先看 GRAY，再查曝光、排线和数据口 |
| 二值图碎点多 | 光照反射、焦距、支架振动、曝光 |
| 双边正常但 ERR 恒偏 | 摄像头机械中心或 `Error Zero` |
| 单边补线偏移错误 | `ROAD_WIDE_NEAR/FAR` |
| EL 长时间卡住 | 当前元素状态机无完整超时，重启摄像头或整机后再检查触发判据 |
