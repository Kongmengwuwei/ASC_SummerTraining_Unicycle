# 视觉调试指南

核对日期：2026-09-07。依据 [image.c](code/image.c)、[perspective.c](code/perspective.c)、[element.c](code/element.c)、[vision_core.c](code/vision_core.c) 和 [board_config.h](code/board_config.h)。

## 1. 当前处理链与坐标

```text
MT9V03X 188×120 整帧 → DMA5 → image_grab
→ 大津二值化 → 逐行提边（种子、上一行搜索窗口、来源标记）
→ 收集 IPM 标定点（有请求时）
→ 元素检测/补线
→ 构建并平滑中线 → 近端窗口拟合 / 点数不足时局部回退
→ 横向误差、航向、曲率、质量、加权方向偏差
→ 发布 CPU0（含元素、绝对限速、跟踪模式、终点请求）
```

算法使用整幅 188×120，列偏移和行偏移都是 0；行 0 为远端，行 119 为近端，中心列 94。当前不是 180×80 ROI，也不是旧八邻域提边实现。

`Left/Right/Mid_Source` 区分 NONE、MEASURED、REPAIRED、PREDICTED；补线和预测点不能被当作独立实测证据用于标定。`Road_Half_Wide[120]` 为统一半宽来源。无 IPM 时允许在非 Run、可信普通道路条件下学习赛宽；Run 冻结学习，有 IPM 时按投影关系处理半宽。没有 Road Near / Road Far 手调参数。

前瞻行 `err_front_row` 默认 67，范围 10–115，是从图像顶部起算的绝对行号。IPM 对边线/中线点集进行坐标变换，不生成整张鸟瞰图。拟合在可用区间评价横向、航向与曲率；窗口不足时退回局部中线和斜率，曲率为 0。

| 量 | 单位与正方向 |
|---|---|
| `g_mid_error` | 前瞻附近中线像素偏差，赛道位于车体左侧为正 |
| `g_track_lateral` | 赛道半宽归一化横向偏差，左正，限 ±2 |
| `g_track_heading` | 航向偏差，左正，限 ±90° |
| `g_track_curvature` | 有符号归一化曲率，左弯为正，限 ±1 |
| `g_direction_camera` | 加权方向偏差，限 ±15000（除以 345 后约 ±43.48 pixel）；不是上述归一化误差 |
| CPU0 `g_dbg_error` | 当前接收的归一化横向误差，不再是旧 7 行平均像素误差 |

CPU1 的 `result.track_valid` 实际发布 `g_direction_valid`，不能只用“中线 ≥12 行且灰度跨度 ≥25”概括所有有效性分支。`my_image.Track_Valid`、方向有效标志、质量和 CPU0 视觉帧年龄应一起判断。

## 2. Image 页与曝光

主菜单 Image 切为横屏；确认循环 BIN+L / BIN / GRAY，上下键曝光 ±2，返回恢复竖屏。曝光默认 48、范围 4–1600；修改的是 RAM 中 `cam_exposure`，需要 Camera Save 或全量 Save 才持久化。

先固定摄像头安装位置和俯角，在 GRAY 检查供电、失焦、反光、过曝与振动，再看 BIN 的赛道分界和 BIN+L 的边线/中线。大津阈值保护为 30–220；不要把旧笔记 80–160 当成所有场景的验收门限。

近端种子搜索区域是行 117 到 100，逐行向远端跟踪，终止行配置为 2。低机位近端边界可能超出视场，应结合真正实测起始行和来源标记判断，不能要求最底两行一定同时有双边。

图像页状态栏提供曝光、阈值、有效性、元素和 CPU0 统计的帧率；帧率窗口为 500 ms，画面最多约每 100 ms 更新，停帧状态栏约每 500 ms 刷新。图像停在最后一帧不代表摄像头仍在采集。

Camera/Element 当前没有旧 G/B/E/M、US/MAX 分段耗时屏显，也不会打开旧 trk 波形；不能按旧文档寻找这些入口。需要性能结论时另行测量，不能仅凭某个固定帧率推断耗时。

## 3. IPM 标定

进入 `Params → Camera → Calib IPM`。车应停车、摆在直道上并对准方向，图像显示标定梯形与角点。

- READY 后确认键请求标定；仅在 IPM 尚无效且 READY 连续约 2 秒时自动请求。
- CPU1 累计 24 个有效直道帧，坏帧跳过，不清空已经累计的有效帧；不是单帧标定。
- 双边有效行至少 20、行跨度至少 30、近/远宽至少 40/18 像素，边线拟合平均残差不超过 2.5 像素等条件共同筛选。
- 完成后 CPU1 发布矩阵和标定序号，CPU0 接收有效矩阵、更新 revision 并标记待保存。
- CPU0 前台 `control_ipm_flush()` 在 STOP 且无 Test/Jog 时仅保存 `ipm_h0…ipm_h8`；失败时保留待保存状态。IPM WR 表示待落盘，IPM OK / IPM SAVED TO FLASH 表示相应就绪/保存状态。
- Camera 页 Save 只存 Exposure 和 Front Row，不包含矩阵；IPM 自动保存、cfg 的 IPM 组或全量 Save 才覆盖矩阵。

当前编译默认已带非零 IPM 矩阵，Flash 还可能覆盖它。`ipm_ok` 只表示算法接受矩阵，不能证明它适合改变后的摄像头安装。安装角度、高度或尺度变更后应重新核验标定。

| 标定提示 | 检查 |
|---|---|
| NO TRACK | 图像与曝光 |
| TOO FEW BOTH-EDGE ROWS | 双边实测范围 |
| SPAN TOO SHORT | 直道可见长度与摆放距离 |
| NOT STRAIGHT | 对齐、弯道、边线噪声 |
| TRACK TOO NARROW/CLOSE | 采样行和赛道宽度 |

## 4. 视觉与控制的接缝

CPU1 发布几何量；CPU0 正式 Run 使用方向偏差、航向、曲率规划转向，结合元素/质量/动量权限规划速度。独立 Balance 不使用视觉转向。具体控制限幅见 [控制调试指南](控制调试指南.md)。

CPU0 每 5 ms 发布运动反馈、参数 revision、IPM 和 Run 标志；CPU1 帧处理开始读取一次快照。元素检测使用本帧原始边线，部分运动判据中的基础 track_error/track_valid 来自上一帧中线结果；元素补线后才统一生成本帧中线。

新 Run 上升沿会重置 CPU1 元素状态；CPU0 要收到带 Run 标志的视觉结果才应用该次终点请求。视觉年龄 ≥100 ms 时结束前进并保留 Balance；丢线先走 Lost 速度和航向保持，连续 600 ms 后退出。程序没有因退出 Run 自动完成全部锁停。

## 5. 元素与跟踪模式

| 编号 | 元素 | 编译默认使能 | 当前处理 |
|---:|---|---:|---|
| 0 | NONE | — | 普通中线 |
| 1 | ZEBRA | 0 | 条纹确认/释放，发布终点请求；CPU0 锁存并按偏移距离停车 |
| 2 | CROSS | 1 | 角点/边线证据与补线，方向使用 HOLD |
| 3 / 4 | RINGL / RINGR | 0 | 里程与独立累计转角状态机，按阶段选单边 |
| 5 | RAMP | 0 | 赛宽、前瞻和运动佐证，按里程/超时退出，发布绝对限速 |

默认只有十字开启；不是“元素全关”。没有路障枚举、检测器或参数。元素输出为 `speed_limit_mps`，不是旧速度倍率。

跟踪模式 0/1/2/3 为 MIDDLE/LEFT/RIGHT/HOLD。左环阶段 3 使用左边线，阶段 1、2、4、5 使用右边线；右环相反。环岛阶段为 0 空闲、1 入环、2 进环、3 沿环、4 环内、5 出环。

现场可调参数仅为四个使能、Zebra Stop、Ring Angle、Ring CntL/R。识别/超时阈值是编译期常量：环岛单状态 500×10 ms=5s，元素屏蔽 80×10 ms=0.8s，十字 1.5s，坡道 5s；这些时间基于 CPU0 uptime，不是图像帧数。连续确认/释放的 FRAMES 常量仍按帧计数，二者要区分。

建议先验证普通直道与单边丢失，再逐个检查元素：正确位置进入、按状态条件退出、普通道路不误报。坡道需要速度/姿态/驱动等运动证据，静止展示一幅坡道图不保证触发；环岛退出涉及 counts 和转角，不能统一要求“过图后一秒内回 NONE”。

## 6. 观测与故障定位

- 图像：IPS200 的 Image/IPM 页；UART2 无连续图传。
- 几何与控制：Run CH14–17 为方向/横向/航向/曲率，CH22 为质量，CH23 为年龄；CH24 含有效性、IPM、HOLD 和元素位。
- 道路状态：hello 后 task 5 Hz、taskevt 事件。曲率分类的左/右弯阈值 0.035/0.020 与 150 ms 防抖仅用于诊断，不反馈给 Run。
- 帧活性：看 frame_seq、age 和 fps；heartbeat 增长只证明 CPU1 主循环活着。
- 采集链路：检查 UART1、VSYNC、PCLK→DMA5、DMA 完成中断及 CPU1 数据段；不要把 CPU1 改成直接访问电机来排查视觉。

低帧率应分别检查曝光、采集丢帧、处理和显示负载；当前不提供逐段耗时遥测。轨迹页面由轮速和航向积分生成相对轨迹，不是相机识别出的绝对赛道地图。软件功能存在不等于元素识别率或实车循迹稳定性已经验收。