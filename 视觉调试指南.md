# 视觉调试指南

视觉与元素跑在 CPU1，结果不进控制层，不改目标航向、目标速度和电机输出。


## 0. 链路与坐标

```text
MT9V03X 188×120 → DMA → 裁剪 180×80 → 大津二值化 → 八邻域左右边线
→ 标准赛宽补中线 → 元素补线 → 重建中线 → 多行平均偏差 → 发布 CPU0
```

裁剪取原图列 `4~183`、行 `40~119`。**算法行 0 是远端，行 79 是近端。**

```text
err = 90 - Mid_Line[i]，取第 27~33 行(共 7 行)平均，再减 err_offset
```

中线偏左 `err > 0`，偏右 `err < 0`。`Track_Valid == 0` 时 `err` 强制为 0。


## 1. Image 页

主菜单 `Image` 进入，切横屏。

| 按键 | 功能 |
|---|---|
| 确认 | `BIN+L → BIN → GRAY` 循环 |
| 上 / 下 | 曝光 ±2，长按连发 |
| 返回 | 退出，恢复竖屏 |

状态栏 `BIN+L  E  48  T118  V1  NONE  F 50`：模式、曝光、本帧大津阈值、`Track_Valid`、当前元素名、帧率。

`F` 统计的是 CPU1 出帧速度，每 500ms 结算一次，与刷屏速度无关。停帧时读数会掉到 0。

`BIN+L` 配色：绿=左边线，蓝=右边线，红=有效中线，黄=双边丢失后沿用的中线。

## 2. 曝光

`GRAY` 模式，车放在赛道上，摄像头装到最终位置和俯角。

默认 48，实测可用范围大致 40~60。上/下键每次 ±2，直到：

- 赛道白色区域不过曝（不是整片纯白），黑线区域不死黑。
- 用手在赛道上快速划过，图像不拖影。
- 车推着走一圈，最亮处和最暗处都不出现整片饱和。

**判据：** 切到 `BIN`，全程 `T` 在 `80~160` 之间浮动，不出现 `V0`，`F` 不下降。

曝光是按行时间计的。调大到超过一帧的时间就会开始掉帧，`F` 掉下来说明已经过头了。

赛道光照不均时优先压低曝光——宁可暗一点，大津会自己找阈值；过曝丢失的信息找不回来。

## 3. 二值与边线

`BIN` 模式，看整帧：

- 赛道内白、赛道外黑，边界清晰。
- 碎点少于十几个孤立像素。碎点多先查支架振动和反光，再动曝光。

切 `BIN+L`，车放直道中央：

**判据：**

- 绿线贴左边界、蓝线贴右边界，从屏幕近端连续延伸到中远端。
- 红中线连续、居中、不抖。
- `V` 恒为 1。
- 车横向平移 2cm，绿蓝红三线整体跟着平移，没有跳变或断裂。

近端两行必须有边线——八邻域起点从这里搜。近端就丢线说明摄像头俯角太小或车体挡住了视野。

## 4. 标准赛宽 `Road Near` / `Road Far`

`Params → Camera` 里的运行参数，步长 1，范围 8~176。默认 `Near=133`(行79)、`Far=30`(行0)，中间线性插值。

改完当帧生效，不用重烧：CPU0 每 5ms 把两个值经信箱发给 CPU1，CPU1 发现变了就重建赛宽表。

它决定单边丢线时中线补在哪，也是环岛单边偏差、坡道超宽、路障收窄的判据基准。

标法（改一次看一次，边改边看）：

1. 车放直道中央，`Image` 页 `BIN+L` 记住红中线的位置。
2. 用纸板遮住**右**边界，让右边线丢失。
3. 红中线应该停在和第 1 步几乎同一个位置。偏左说明赛宽标大了，偏右说明标小了。
4. 回 `Params → Camera` 调 `Road Near`，再进 `Image` 页看。近端标完再遮远端标 `Road Far`。
5. 标完 `Params → Save`。

**判据：** 单边遮挡前后，红中线横向偏移小于 5 像素（屏幕上约 9 个点）。

## 5. 偏差零点 `err_offset`

进 `Params → Camera` 就会自动拉起摄像头，屏幕下方两行一直在刷：

```text
TH 118 VAL 1 LOST   0 FPS 50
ERR    0.312  L  0 R  0
```

`Error Zero` 步长 0.1。

1. 车放直道正中央，车身对准赛道方向。
2. 调 `Error Zero`，直到 `ERR` 稳定在 0 附近。
3. 车整体左移 3cm，`ERR` 应变正；右移 3cm，应变负。**符号错了不要靠 `Error Zero` 补**，是摄像头装反的问题。
4. `Params → Save` 保存，断电重上电确认值还在。

**判据：** 直道静止 `|ERR| < 2`，推车走直道全程 `|ERR| < 8`，`LOST` 恒为 0，左右平移符号正确。

## 6. 循迹增益 `track_err_gain`

接缝只有一句：`g_yaw_target = imu_get_angle_yaw() + err * TRACK_ERR_GAIN`。

单位是"每 1 像素偏差对应多少度航向修正"。`Track Gain` 步长 0.05，默认 1.0。

从 0.3 起，架空转动车体看 `yaw` 波形响应，再落地低速跑：

- 走直道画龙 → 调小。
- 入弯切内道或压线 → 调大。

**判据：** 低速跑完一圈直道加弯道，`trk` CH1 (`Track_Valid`) 不出现 0，CH7 (连续无效帧) 恒为 0。

## 7. `trk` 波形通道

进 `Params → Camera` 或 `Params → Element` 自动打开，退出即关。115200，FireWater。

| CH | 内容 | 正常值 |
|---:|---|---|
| 0 | 中线偏差 | 直道 ±2 |
| 1 | `Track_Valid` | 恒 1 |
| 2 | `Search_Stop_Line` 有效前瞻行数 | > 40 |
| 3 | 左丢线行数 | 直道 0 |
| 4 | 右丢线行数 | 直道 0 |
| 5 | 双丢线行数 | 恒 0 |
| 6 | 大津阈值 | 80~160 |
| 7 | 连续无效帧数 | 恒 0 |
| 8 | CPU1 心跳 | 持续增加 |
| 9 | 距上一帧结果的时间(ms) | 周期回到小值 |
| 10 | 元素编号 | 见下表 |
| 11 | 元素建议速度倍率 | 1.0 |
| 12 | 元素停车请求 | 0 |

CH8 只说明 CPU1 主循环活着。判断摄像头是否持续出帧看 CH9。

`Track_Valid` 的两个条件：有效中线行数 ≥ 12，且本帧灰度跨度 ≥ 25。

## 8. 元素

普通循迹跑稳后再开，`Params → Element` 一次只开一个。默认全关。

| EL | 元素 | 名称 | 当前处理 |
|---:|---|---|---|
| 0 | 无 | `NONE` | 普通中线 |
| 1 | 斑马线 | `ZEBRA` | 置 `stop_request=1` |
| 2 | 十字 | `CROSS` | 按角点延长边线 |
| 3 | 左环岛 | `RINGL` | 编码器+独立转角状态机、单边偏差 |
| 4 | 右环岛 | `RINGR` | 同上 |
| 5 | 坡道 | `RAMP` | 发布速度倍率 |


`Params → Element` 页进去就一直显示：

```text
ELEM RINGL   RING 2
SCALE 0.70  STOP 0
```

调法：把对应元素单独摆在直道上，推车匀速通过，看这两行；要同时看图像就进 `Image` 页看状态栏最后一列的元素名。

**判据：**

- 通过元素时元素名在正确位置出现，通过后 1 秒内回到 `NONE`。
- 直道和普通弯道上不误触发。
- 元素段内 CH1 不掉 0。

互斥关系：环岛中不判十字，十字或坡道中不判环岛，三者任一进行中不判路障。环岛单状态超过 `Ring TmO` 帧强制回空闲。任何元素退出后有 `Guard Cnt` 帧屏蔽期，只挡新进入。

阈值全是未标定初值，逐个实车标。

已知：`ZEBRA` 触发后 `stop_request` 会一直保持 1，只能靠断电、重新初始化或 `En Zebra` 关再开来清。Run 未接入所以不影响行车。

## 9. 故障对照

| 现象 | 先查 |
|---|---|
| `CAMERA INIT FAILED` | 电源、UART1 `P02_2/P02_3`、VSYNC `P02_0`、PCLK `P02_1`、D0-D7 |
| 长时间 `CAMERA STARTING` | CPU1 是否跑起来、摄像头配置通信 |
| `F` 掉到 0 但 CH8 还在增加 | VSYNC、PCLK、DMA5、DMA 完成中断 |
| `F` 比正常值低一截 | 曝光调太大导致掉帧，或 CPU1 单帧处理超时 |
| 全黑或全白 | 先看 `GRAY`，再查曝光、排线 |
| 碎点多 | 反光、失焦、支架振动，最后才是曝光 |
| 双边正常但 CH0 恒偏 | 摄像头机械中心，再调 `Error Zero` |
| 遮单边后中线跳走 | `Road Near` / `Road Far` |
| 近端就丢线 | 俯角太小或车体挡视野 |
| 元素名卡住不回 `NONE` | `Ring Timeout`、`Elem Guard`，或该元素触发判据太松 |

## 10. 当前限制

- 偏差尚未写入 `g_yaw_target`，`Run` 未启用。
- 速度倍率和停车请求未进控制层。
- 无图传，图像只在 IPS200 上看。
- 大津阈值无跨帧滤波。
- ROI 和前瞻行固定，无逆透视、无动态前瞻。赛宽表是线性插值，不是真实透视曲线。
- `Search_Stop_Line` 上限 78。
- Image 页图像可能停在最后一帧，但状态栏每 500ms 会刷一次，看 `F` 是否掉到 0。
